# 01 — Sản phẩm / Product: bốn lời hứa với bà con

> **Mục đích / Purpose**: Định nghĩa canonical về sản phẩm ForFish — bốn lời hứa, thứ tự build, nguồn dữ liệu và quy tắc adapter, vòng lặp giá trị giữa các trục.

**Load khi / Load when**: cần hiểu scope một trục, quyết định feature thuộc trục nào, đụng tới data vendor (OceanByte, SDWork), hoặc viết copy/positioning.

---


## Ai dùng được — CẦN TÀI KHOẢN, DO SDVICO CẤP (chủ dự án chốt 2026-09-01)

App **yêu cầu đăng nhập** mới dùng được; **không có đường tự đăng ký** — tài khoản do SDVICO cấp, bà con gọi `0939 243 222`. Trước 2026-09-01 app dùng được không cần tài khoản (demo mode localStorage), chỉ khoá vài khối giá trị cao.

**Chưa có tài khoản thì thấy gì:**

| Mở | Khoá |
|---|---|
| Trang chủ `/` — hero, tin bão, Bốn việc, hộp thư, **và lời nhắc cài PWA** (bản web) | `/ngu-truong` bản đồ ngư trường |
| `/login` · `/quen-mat-khau` · `/quyen-rieng-tu` | `/tau` hồ sơ tàu · `/nguoi` sổ thuyền viên · `/cang` danh bạ cảng |
| `/tien` mục **Giá cá** (số công khai VASEP) | `/tien` mục Tin mua/bán · Bán ở đâu |
| `/dang-ky` — GIỮ MÀN nhưng khoá, chỉ còn câu "gọi SDVICO" | |

**Vì sao trang chủ mở nguyên:** người chưa có tài khoản phải NHÌN THẤY app làm được gì rồi mới có cớ gọi xin cấp. Tin bão để mở vì đó là chuyện an toàn tính mạng — không đem ra làm mồi câu tài khoản. Giá cá mở vì là số công khai của VASEP, khoá cũng không giữ được gì.

**Vì sao GIỮ `/dang-ky` mà không xoá route:** đường đó đã phát ra ngoài (tin nhắn nhân viên, ảnh chụp màn, chính câu "Đăng nhập / Đăng ký" trong app cũ). Xoá là bà con bấm vào ra 404 đúng lúc đang cần tài khoản.

**DẢI KHẨN ẨN KHI CHƯA ĐĂNG NHẬP (2026-09-01b).** Chủ dự án gửi ảnh dải khẩn đầy "Thay lọc dầu · Quá hạn 93 ngày" và bảo *"bỏ các loại dữ liệu seed này đi"*. **Không còn seed nào trong code** — đợt gỡ 2026-07-29 đã làm sạch (`boats.ts` ghi rõ "KHÔNG seed tàu mẫu"); thứ trong ảnh là dữ liệu THẬT gõ tay còn lại trong máy đó, đọc thẳng từ localStorage. Nhưng nó phơi ra lỗi của chính đợt khoá đăng nhập: cả app đã đòi tài khoản mà trang chủ vẫn bày lịch bảo dưỡng + giấy tờ + nợ SDVICO của MỘT chủ tàu nào đó cho người chưa đăng nhập — máy dùng chung ở bến, hay máy vừa cài lại, là thấy việc của người khác. Nay `UrgentStrip` trả `null` khi chưa có chuỗi cứng.

**HỘP THƯ THÌ KHÔNG** áp cùng luật: tin gửi CHUNG tới máy chưa gắn tài khoản là ca đã cố ý mở (sửa 2026-08-01n) — giấu đi là mở lại đúng cái lỗ nó sinh ra để bịt.

**Ngoài biển mất sóng KHÔNG bị khoá:** cổng đọc `signedIn` của `use-auth` = *có phiên HOẶC có chuỗi cứng trong máy*. Chuỗi nằm sẵn ở máy nên mất sóng nhiều ngày vẫn vào được — chặn giữa biển là chặn đúng lúc cần bản đồ và cảnh báo ranh giới nhất. Chốt thật vẫn ở API + RLS khi có sóng.

## 1. ForFish là gì

App đồng hành của **ngư dân Việt Nam**, do **SDVICO** đặt hàng. Mobile-first, tiếng Việt đời thường.

> **Định vị bổ sung (2026-06-16)**: SDFish còn là **app khách hàng của công ty** — mọi KH mua hàng (chiến dịch anten vệ tinh: mỗi đơn ra trực tiếp KH yêu cầu cài app) đăng nhập bằng **SĐT + mật khẩu** (sale báo khi mua; KHÔNG email/OTP) để theo dõi **thiết bị · bảo hành · hỗ trợ kỹ thuật**. Dữ liệu KH **TÁCH RIÊNG SDWork**: SDFish có DB của riêng nó, SDWork **đẩy webhook** sang khi có đơn (không đọc-live). Chi tiết: [04-data-model §5b](04-data-model.md) + [contract webhook](../integration/sdwork-sso-contract.md). Public + khu khách hàng giữ nguyên (đăng nhập mới thấy đồ của mình).

**Người dùng**: ngư dân, phần lớn 40–60 tuổi, **ít rành công nghệ** (low tech literacy). Mọi quyết định sản phẩm phải qua filter: *"bác ngư dân 55 tuổi, tay ướt, đứng ngoài nắng có dùng được không?"* — chi tiết UI ở [03-design-system.md](03-design-system.md).

**Nguyên tắc số 1**: sản phẩm cấu trúc quanh **BỐN LỜI HỨA (bốn trục)** — KHÔNG phải quanh feature, KHÔNG phải quanh nguồn dữ liệu. Vendor có thể thay, lời hứa thì không.

> ## ⚓ PHẠM VI OFFLINE — chủ dự án chốt 2026-08-17
>
> **"Offline ra khơi rồi thì cần gì các tin chợ, đơn, cửa hàng… chỉ cái gì đã lưu trong memory thôi, đừng để nó truy vấn internet làm treo lỗi app là được."**
>
> Lời hứa offline chỉ phủ **thứ dùng ngoài biển**: dự báo/bản đồ đã tải, tin bão, tủ giấy tờ, sổ thuyền viên, tàu, bảo dưỡng, điểm ghim, hộp thư đã tải, dẫn đường bằng GPS.
>
> **XEM ĐƯỢC BẢN ĐÃ TẢI** (bổ sung 2026-08-18 — *"cửa hàng nó ít đổi món và đơn, nên cứ xem bình thường, online lại thì tự động tải mới"*): **danh mục Cửa hàng** + **đơn của tôi**. Hai thứ này ít đổi và nhỏ (vài chục KB) nên lưu bản đã tải, mất sóng xem bình thường kèm dòng "đang xem bản lưu lúc …"; **đặt/huỷ vẫn cần sóng**.
>
> **KHÔNG nằm trong lời hứa offline** (khu "chuyện ở bờ", không lưu bản nào): chợ tin mua/bán · bảng giá live · hỏi mua · cảnh báo thuyền viên chéo. Mất sóng thì các mục này **chỉ cần hai điều**:
> 1. **KHÔNG treo, KHÔNG làm sập app** — mọi lời gọi mạng phải có **đồng hồ + `.catch` + nhánh nói thật**. Ca chết người là `fetch` không đồng hồ ở sóng "sống mà chết" (bắt tay được, gói tin không về): promise không settle, màn đứng vĩnh viễn.
> 2. **Nói đúng lý do** — "chưa tải được, máy đang không có sóng", KHÔNG được đội lốt "chưa có gì" / tin mẫu / dữ liệu giả.
>
> 3. **Có sóng lại thì TỰ đồng bộ** — màn đang mở nghe `online` rồi tự tải lại, không bắt bà con bấm "Thử lại".
>
> ⛔ **KHÔNG làm**: cache tin chợ (tin đổi từng ngày, lưu là dễ nói dối về tin còn hiệu lực) · outbox/hàng đợi gửi lại cho đặt hàng – đăng tin – báo cáo. Luật chung khi cân nhắc lưu thêm: **chỉ lưu thứ NHỎ và ÍT ĐỔI**; thứ nặng hoặc đổi từng giờ thì không, vì mọi kho dùng CHUNG một hạn ngạch theo origin (WebKit) — lấy chỗ của gói dự báo là lấy chỗ của thứ giữa biển không tải lại được.
>
> Chi tiết bất biến + trần thời gian từng đường: [ADR 0004](../adr/0004-pham-vi-offline-chi-du-lieu-di-bien.md).

## 2. Bốn trục / The four promises

> **Lời hứa ≠ route** (cập nhật 2026-06-15): nav đã đổi sang **hướng đối tượng** (dock: Trang chủ · Ra khơi · Tàu cá · Bạn thuyền · Giao dịch — nhãn 2026-07-28), KHÔNG còn 1 route/trục. Bản đồ lời-hứa → nơi ở thật: **Trục 1** `/ngu-truong` · **Trục 2** `/tien` (tab Giao dịch) · **Trục 3** `/tau` (tab Dịch vụ/Sản phẩm) · **Trục 4** `/tau` (tab Giấy tờ) + `/nguoi` (thuyền viên). Route cũ `/gia-ca` `/van-hanh` `/giay-to` `/thuyen-vien` = **redirect**. Nguồn đúng về nav: [07-design-spec §4](07-design-spec.md). Lời hứa thì không đổi — đó mới là trục.

### Trục 1 — Đánh bắt tốt hơn (`/ngu-truong`)
- **Hứa gì**: ra khơi trúng hơn, đỡ phí dầu phí công.
- 💰 **PHÂN HẠNG TÀI KHOẢN (chủ dự án chốt 2026-07-26 — THAY mô hình teaser 06-11)**: **dự báo cá (PFZ)** và **dự báo thời tiết QUÁ 3 NGÀY** (ngày 4–16 ở dự báo biển + khung 5/7/10/16 ngày trên bản đồ) là tính năng **PREMIUM** — khoá HẲN (không còn heatmap teaser). **Thêm 2026-08-29 (user chốt)**: ba công cụ bản đồ **Đến điểm · Điểm đã lưu · Dẫn đường** cũng là PREMIUM — nhưng **ẨN HẲN nút khỏi acc thường** (không bày ổ khoá như lớp cá, vì là công cụ: ẩn cho gọn rail; gate `premiumTools = fishAccess==="open"` ở `ra-khoi-controls.tsx`, xem [07 §11](07-design-spec.md)). Ba nấc: **chưa đăng nhập** → thẻ khoá mời Đăng nhập · **hạng thường (basic)** → thẻ khoá "gọi SDVICO nâng cấp" (`SDVICO_HOTLINE`) · **premium** → mở hết. Miễn phí cho mọi người: điều kiện biển 3 ngày đầu, tin bão, bản đồ hải đồ/vệ tinh, gió sóng theo điểm, cá-mùa-này (Đến điểm · Điểm đã lưu · **dẫn đường** đã chuyển sang PREMIUM từ 2026-08-29). KHÔNG có luồng thanh toán trong app — premium gán qua **webhook SDWork** (`tier`/`premiumUntil`) hoặc **web quản trị `/quan-tri`** (độc lập về giao diện, chung deploy/DB). Chốt thật ở middleware (401/403 `/api/fish-forecast`); luật hạng `src/lib/tier.ts` (`resolveTier` + `featureAccessDecision`, thuần, có test); UI khoá = `PremiumLock` ([07 §trạng thái khoá](07-design-spec.md)). Demo mode (chưa cấu hình Supabase) mở hết. **Đường lùi MẤT SÓNG (2026-07-26)**: `getUser()` cần mạng để xác thực → ra khơi mất sóng trả `user=null`, premium đã trả tiền bị coi như đăng xuất → **mất bản đồ cá đã tải sẵn ở bờ**. Nay `useFeatureAccess` ghi "dấu premium" vào máy (`forfish.tier.premium.v1`) mỗi lần tra ĐƯỢC lúc còn sóng; mất sóng + có dấu → cho xem tiếp (`featureAccessDecision`). KHÔNG phải bypass: chốt thật vẫn ở middleware/RLS khi có mạng, offline SW chỉ trả đúng thứ đã tải hợp lệ lúc còn premium; đăng xuất thật (đang có sóng) thì xoá dấu.
- **Gồm**: bản đồ ngư trường map-first (lớp hải đồ/vệ tinh + nhãn chủ quyền tiếng Việt + **tên đảo đầy đủ** ven bờ/Hoàng Sa/Trường Sa + **tuyến hàng hải, luồng lạch tham khảo**), **dự báo vẽ động kiểu Windy** (lớp gió/sóng theo giờ, **chọn khung 3/5/7/10/16 ngày**, thanh thời gian + nút chạy), gió sóng theo điểm chạm 1–16 ngày, **lớp "Cá mùa này"** (vùng nào đang vụ cá gì — mùa vụ nhiều năm), dẫn đường tiết kiệm dầu.
- ⚠️ **TRIẾT LÝ (user chốt 2026-06-10): app KHÔNG phán "đi hay không đi"** — ngư dân có lịch chuyến của họ; đã bỏ điểm số 1–100 + lời khuyên ra khơi khỏi UI bản đồ. App mô tả ĐIỀU KIỆN ("Biển êm/Biển động nhẹ/Biển động mạnh" + sóng m + gió cấp), quyết là việc của thuyền trưởng. `scoreDay` trong `sea.ts` vẫn dùng nội bộ để tô màu mức.
- ⛔ **KHÔNG LÀM — lớp "ngày nào ra đánh được" (operability theo nghề)** (user chốt 2026-07-26). Đã có đề xuất (từ phiên phản biện 3 chuyên gia + mạch Copernicus): tính theo NGHỀ (vây/chụp mực/câu/rê) xem sóng gió ngày nào thì nghề đó làm được, gắn vào thanh ngày 10–16 ngày, lấy đó làm "dự báo cá 10 ngày trung thực" thay cho thứ không đo được. **User BÁC: "họ đi họ tự có chọn, ko đưa vào app làm gì"** — ngư dân đi biển hàng chục năm TỰ BIẾT nghề mình chịu được sóng gió cỡ nào; app bày thêm lớp đó vừa THỪA vừa dạy đời. App đã đưa đủ dữ kiện để thuyền trưởng tự quyết: sóng (m) + gió (cấp) + mô tả mức biển + mưa/dông theo giờ tới 16 ngày + độ tin từng ngày. Đây là **hệ quả trực tiếp của TRIẾT LÝ ngay trên** — cùng một nguyên tắc: app đưa DỮ KIỆN, không đưa PHÁN QUYẾT. KHÔNG mở lại trừ khi chính bà con yêu cầu.
- **Dữ liệu hiện chạy** (đều miễn phí, không cần key, qua adapter trong `src/lib/`):
  - Ảnh vệ tinh: chương trình mở của NASA (cập nhật hằng ngày, **trễ ~2 ngày** — UI ghi rõ "ảnh ngày X, chậm vài ngày"). Lớp dòng chảy mặt biển: chưa có nguồn tile miễn phí không-key → làm sau.
  - Dự báo vẽ trên bản đồ (kiểu Windy): lưới ~80 điểm Open-Meteo theo GIỜ, **bà con chọn khung 3/5/7/10/16 ngày** (`GRID_DAY_OPTIONS`), mũi tên hướng + màu theo độ dữ (`src/lib/forecast-grid.ts`). Bước thời gian TĂNG DẦN để không phình tải/khung: ≤3 ngày mỗi 3h · 4–7 ngày mỗi 6h · >7 ngày mỗi 12h (`stepHourIndices`). Sóng dùng model `ncep_gfswave025` (best-match sóng theo giờ chỉ ~9 ngày); gió best-match phủ 16 ngày. Tầm càng xa càng kém tin — cùng lời dặn như dự báo điểm.
  - **DỰ BÁO CÁ (PFZ — tính năng đầu bảng)**: `src/lib/fish-predict.ts` + route `/api/fish-forecast` (cache 6h) — chấm điểm từng ô biển 0.25°: **`fit = cổng-nhiệt(trapezoid SST) × mồi(giới hạn mềm) × habitat × cổng-độ-sâu × mùa-vụ(prior mềm)`**. **Đổi công thức tổng hợp (VIỆC 3, 2026-07-25)**: TRƯỚC dùng TRUNG BÌNH CỘNG có trọng số của 6–7 yếu tố → nén phương sai (gần hết ô rơi 0.3–0.5, ~½ vùng biển thành điểm nóng, các loài ra bản đồ na ná). NAY: mồi (chlorophyll) tách ra làm **giới hạn mềm** (`foodLimiter = FOOD_FLOOR + (1−FOOD_FLOOR)·mồi`, mồi=0 chỉ hạ điểm không zero hẳn), còn các **cơ chế gom cá — front nhiệt · front mồi · rìa xoáy (SSHA) · nước trồi (dị thường nhiệt ÂM, Coral Reef Watch 0.05°, trễ ~1 ngày) · hội tụ dòng chảy mặt (u,v **Copernicus dòng TỔNG 1/12°** → convergence — xem "ĐỔI NGUỒN HỘI TỤ" bên dưới) · tầng nhiệt** — tổng hợp bằng **SOFT-OR** (`softOrHabitat`: `1−Π(1−scale·w/wMax·x)`, "chỉ cần MỘT cơ chế mạnh là đủ sáng ô", hết bị nhiều yếu tố yếu pha loãng). Trọng số riêng từng loài (`SPECIES_PROFILES.w`), loài ưa nước trồi lạnh thì lấy max(rìa xoáy, độ lõm mực nước). Hằng (`SOFTOR_SCALE 0.4 · AGG_FLOOR 0 · FOOD_FLOOR 0.45 · NEUTRAL_AGG 0.6`) **chốt bằng calibrate trên lưới THẬT** (`scripts/fish-predict-viec3-calib.mjs`): diện tích điểm nóng s≥50 co ~27→18% (t7)/36→27% (t1), median 42→36, cá đáy mùa vụ KHÔNG biến mất. ⚠️ **SỬA SỐ SAI (2026-07-26)**: con số "18%" ở trên đo bằng script calibrate vốn **CỐ Ý BỎ QUA tầng nhiệt HYCOM** — nên nó KHÔNG phải %điểm nóng của sản phẩm. Đo trên **API THẬT đang chạy** (`GET /api/fish-forecast`, 2354 ô biển, ảnh 23–24/7/2026, đủ 7 nguồn kể cả HYCOM) thì %điểm nóng thực tế là **49,0%** (1153/2354) — gần nửa vùng biển đỏ, vô dụng cho bà con. Thủ phạm là `thermoFit` (xem mục TẦNG NHIỆT bên dưới); sau khi sửa còn **21,1%** (497/2354), median 49→37, p90 66→60, KHÔNG loài nào biến mất. Bài học đã lặp 3 lần: **calibrate phải đo trên API thật, không đo trên bản sao thiếu nguồn.** **ĐỘ LỚN nước trồi + xoáy lạnh nay so nước với VÙNG BÊN CẠNH (VIỆC 2, 2026-07-25)**: hai yếu tố `upwTerm` (dị thường nhiệt) và `coldStrength` (độ lõm SSHA) TRƯỚC lấy dị thường so NHIỀU NĂM / CẢ BỒN (anomaly Coral Reef Watch, SSHA thô) → sáng-tối ĐỒNG LOẠT toàn miền theo mùa, KHÔNG xếp hạng được ô nào hơn ô nào (chỉ là núm chỉnh độ sáng, phình-co vùng đỏ vô cớ). NAY dùng **dị thường KHÔNG GIAN** (`spatialAnomaly`: mỗi ô trừ TRUNG VỊ các ô trong bán kính `SPATIAL_RADIUS_DEG = 2.5°`) → bỏ phần nền cả-bồn, chỉ còn cấu trúc nước trồi/xoáy ĐỊA PHƯƠNG. Rìa xoáy (`eddyEdge` = gradient SSHA) GIỮ NGUYÊN. Hằng `UPW_SCALE 0.55 · COLD_SCALE 0.09` ≈ p90(|dị thường không gian|) trên 30 ngày THẬT (`scripts/fish-predict-viec2-spatial.mjs`). Bằng chứng: std KHÔNG GIAN của `upwTerm` giữa các ô 0.07→0.31 (mùa hè anomaly dương cả bồn → `upwTerm` cũ ~0 KHẮP NƠI, chết như tín hiệu; nay trải rộng, "lần đầu chỉ vào chỗ cụ thể"); %điểm nóng CŨ→MỚI 14.9→16.6% (Δ+1.7, KHÔNG phình). Ghi chú: trong cửa sổ hè, mùa SSHA cả-bồn yếu (std giữa-ngày 0.011 m) nên `coldStrength` gần như không đổi — dị thường không gian ở đây vô hại/đúng nguyên tắc, sẽ có ích khi nền SSHA mùa mạnh. Ngưỡng giữ ô hạ về 25 (client tự lọc theo **sàn hiển thị 50**). SST + phù du BẮT BUỘC; 4 trường còn lại TUỲ CHỌN — thiếu trường nào thì loại yếu tố đó khỏi soft-OR. **THIẾU NGUỒN PHẢI LÀM ĐIỂM GIẢM, KHÔNG ĐƯỢC TĂNG (sửa 2026-07-26)**: trước đây mốc chuẩn hoá `wMax` được suy từ các yếu tố CÒN LẠI và cổng độ sâu thiếu dữ liệu thì ×1 ("không phạt oan") — hệ quả là **nguồn chết làm bản đồ SÁNG HƠN**: (a) mất một lưới ⇒ term của nó biến khỏi mảng ⇒ `wMax` tụt ⇒ các term còn lại được nhân lên (đo thật: cá ngừ mắt to, `w.thermo = 0.5` là trọng số lớn nhất, ô front mạnh nhưng D20 không hợp — HYCOM chết thì `agg` 0.240 → 0.343, **+43%**); (b) mất lưới ETOPO ⇒ ô nước cạn từ ×0 nhảy lên ×1 ⇒ cá ngừ/cá xa bờ hiện SÁT BỜ (đúng lỗi commit 83a4e7e đã sửa; đo thật: 637 ô×loài TĂNG điểm, cao nhất 0→86). NAY: (1) **`wMax` CỐ ĐỊNH theo HỒ SƠ LOÀI** (`speciesWMax` = max toàn bộ trọng số cơ chế KHAI BÁO — thermFront/chlFront/eddy/upw/conv/thermo, kể cả cơ chế hôm nay thiếu dữ liệu; `w.food` không tính vì mồi là giới hạn mềm), precompute 1 lần lúc nạp module ⇒ bỏ bớt term chỉ bỏ bớt thừa số trong tích ⇒ `agg` GIẢM hoặc GIỮ, **không bao giờ tăng** (test "monotonic" khoá tính chất này); (2) **cổng độ sâu khi KHÔNG BIẾT độ sâu** (mất ETOPO, hoặc ô ETOPO NaN) dùng `DEPTH_UNKNOWN_FIT = 0.5` thay vì 1 — "không biết thì bớt chắc chắn", không phạt tới 0 (không có bằng chứng ô này cạn) mà cũng không thưởng đủ (không có bằng chứng ô này sâu). **Căn cứ số** (`scripts/fish-predict-wmax-calib.mjs`, ETOPO THẬT 2354 ô biển VN): kỳ vọng `deepWaterFit` theo dải offshore = 0.578–0.716, trung bình **0.63** = mức KHÔNG THIÊN VỊ → chốt 0.5, thấp hơn có chủ đích. **Đo TRƯỚC/SAU trên lưới THẬT** (3 ngày 21–23/7/2026 + 1 ngày 15/1/2026, cùng dữ liệu cache): ĐỦ NGUỒN gần như KHÔNG ĐỔI (median/p90/p95/max y hệt, %điểm nóng 48.8→48.8 · 47.9→47.9 · 49.6→49.6 t7 và 66.6→66.5 t1; chỉ 3–4 ô ETOPO-NaN sát bờ của loài xa bờ bị hạ) — đúng thiết kế: wMax cố định BẰNG wMax suy-ra khi mọi nguồn còn sống. Khi HỎNG NGUỒN, số ô×loài **ĐIỂM TĂNG**: mất HYCOM 1→**0**, mất dị thường nhiệt 430→**0**, mất ETOPO 637→164 (mức tăng cao nhất 86→**43 < sàn hiển thị 50** — cá ngừ không thể tự dựng lại điểm nóng sát bờ; phần còn lại là ô ĐÃ BIẾT cạn ×0 nay thành "không biết" ×0.5, không thể tránh trừ khi xoá hẳn loài). ĐÁNH ĐỔI đã đo: mất HẲN ETOPO thì loài xa bờ vẫn còn trong payload (≥25: ngừ vây vàng ~600, mực xà ~440, nục heo ~640 ô) nhưng gần như hết ô ≥50 → ở sàn hiển thị 50 bản đồ sẽ THƯA cá xa bờ (t1: %điểm nóng 67.8→33.0). Đây là hành vi ĐÚNG (mất bằng chứng thì bớt khẳng định) chứ không phải lỗi. **TÍNH TRÊN TOÀN BỘ VÙNG BIỂN VN (sửa 2026-06-11, user: "ko được giới hạn mấy vùng khoanh sẵn")**: trước đây chỉ chấm ô NẰM TRONG 7 đa giác `FISH_REGIONS` thô → bỏ trắng phần lớn EEZ (chỉ vài mảng có dự báo). Nay chấm MỌI ô biển VN: `nearestRegionWithin(lat,lon,2°)` gán vùng GẦN NHẤT (phủ kín, không lỗ hổng; null khi xa >2° mọi vùng = nước ngoài) → mùa vụ + vùng chỉ còn là BỘ LỌC LOÀI, không phải giới hạn tính toán. Đã bỏ viền 7 vùng khoanh trên bản đồ (gây hiểu lầm). Kiểm chứng: tháng 6 cells 803 → 2.152, phủ 102–117,5°E × 5–21,5°N. Lọc mùa vụ (`fish-seasons.ts`). Nguồn lưới NOAA ERDDAP công khai (coastwatch): SST blended không lỗ mây + phù du DINEOF vá mây + SSHA blended + `noaacrwsstanomalyDaily`; **dòng chảy đổi sang Copernicus Marine** (`cmems_mod_glo_phy_anfc_merged-uv_PT1H-i`, `utotal/vtotal` 1/12°). **ĐỔI NGUỒN HỘI TỤ + HIỆU CHỈNH LẠI (2026-07-26)**: `noaacwBLENDEDNRTcurrentsDaily` khai `surface_geostrophic_*_sea_water_velocity` — dòng ĐỊA CHUYỂN về mặt vật lý gần như KHÔNG PHÂN KỲ, nên `-(∂u/∂x+∂v/∂y)` trên nó là **NHIỄU VI PHÂN SỐ**, không phải nước dồn thật. Bằng chứng (`scripts/copernicus-probe.mjs`, `scripts/conv-copernicus-calib.mjs`): **tự tương quan KHÔNG GIAN trễ-1 ô của trường phân kỳ** trên hộp biển VN — NOAA **−0,029** (răng cưa, không cấu trúc) vs Copernicus dòng TỔNG **+0,732** ở 1/12° (**+0,510** ở 1/3°). Chọn asset `timeChunked` (1/12°, 3,83 MB, 1,2–4,0 s) chứ KHÔNG `downsampled4` (1/3°, 1,33 MB) vì 1/12° **mịn hơn lưới cá 0,25°** ⇒ hội tụ tính TRÊN LƯỚI GỐC rồi mới lấy mẫu xuống (đúng chiều vật lý), còn 1/3° thô hơn lưới đích = làm mượt trước rồi mới đạo hàm. **NOAA KHÔNG giữ làm dự phòng** — lùi về nhiễu tệ hơn không có: Copernicus hỏng ⇒ **bỏ HẲN term `conv`**, bất biến monotonic (f0b907d) bảo đảm mất nguồn làm điểm GIẢM (đo thật kịch bản chặn host: route vẫn 200 trong 1,17 s, ô ≥50 514→454, `dataQuality` 1→0,95, KHÔNG loài nào biến mất). Cặp u,v NOAA đã **gỡ khỏi route** (bớt 2 fetch). **HẰNG PHẢI HIỆU CHỈNH LẠI** vì `w.conv` của 40 loài từng đặt khi `conv` chấm nhiễu: hằng cũ `0,1` là "m/s mỗi Ô" nên **lệ thuộc bước lưới** (0,25° → 1/12° là cùng dòng chảy nhưng chênh mỗi ô nhỏ đi 3 lần). NAY ghi theo ĐỘ — `CONV_FULL_PER_DEG` × `gridStepDeg(cur.u.lats)`. Số đo (hội tụ THÔ lấy mẫu về ô cá, chỉ phía hội tụ >0): p90 NOAA **0,2218** vs Copernicus **0,4395** m/s/độ. Hằng cũ 0,4/độ = **1,80 × p90 của chính nguồn nó** ⇒ giữ nguyên tỷ lệ trên nguồn mới: 0,4395 × 1,80 ≈ 0,79 → chốt **`CONV_FULL_PER_DEG = 0,8`**. KHÔNG lấy thẳng p90 = 0,44 (luật `UPW_SCALE`/`COLD_SCALE`) vì đo trên ĐIỂM CUỐI thì nó làm %điểm nóng **PHÌNH** 21,2→23,2 · 21,0→23,4 · 29,5→31,7. Ở 0,8 phân bố `convTerm` khớp gần y hệt nguồn cũ (mean 0,1214 vs 0,1216; p90 0,3662 vs 0,3720) — yếu tố hội tụ **không to lên, chỉ ĐÚNG CHỖ hơn**. **TRƯỚC/SAU trên lưới THẬT** (2 ngày hè + 1 ngày đông, cùng dữ liệu): %điểm nóng 21,2→21,6 · 21,0→21,7 · 29,5→28,7; median 37→37 · 37→37 · 44→43; **KHÔNG loài nào biến mất** (số loài đạt sàn hiển thị giữ nguyên 23 hè / 21 đông ở MỌI kịch bản). **Đo trên API THẬT** (`GET /api/fish-forecast`): 1,27 s → 4,07 s lần lạnh (0,88 s lần ấm, ngân sách 60 s), payload 344 → 345 KB, ô ≥50 494 → 514, `dataQuality` 1 → 1, `sources.currents` từ ảnh **2 ngày tuổi** thành **ageDays 0**. `w.conv` của 40 loài **KHÔNG đổi một số nào** — giáp xác giữ nguyên ~0,12 (loài ĐÁY, dòng MẶT không gom chúng). Chi tiết + bảng số: [ops/external-services.md](ops/external-services.md). **Bộ loài rộng (40 loài; cá ngừ đại dương TÁCH vây vàng + mắt to 2026-07-25) — để ~90% bà con tìm được loài mình đánh**: `SPECIES_PROFILES` phủ 6 nhóm `category` — cá nổi lớn (ngừ vây vàng/**mắt to**/vằn/chù/ồ/chấm, thu, cờ, nục heo, ngân), cá nổi nhỏ (cơm, trích, nục, bạc má, tráo, sòng, chỉ vàng, lầm, đối), mực & bạch tuộc (ống, xà, lá, nang, bạch tuộc), cá đáy (mối, đổng, phèn, đù, khoai, chim, bơn, hố), cá rạn (hồng, mú, kẽm), giáp xác (tôm bạc, tôm sú, ghẹ xanh, cua biển, ruốc). SST/dải mồi/đặc tính từ FAO + FishBase + RIMF (agent khảo cứu, có nguồn). **TRUNG THỰC theo `surfaceSignal`**: cá nổi (high) dự báo điểm tin được; loài ĐÁY/RẠN/cửa sông (low) ảnh vệ tinh mặt biển ít giúp → habitat kéo về trung tính (`SURFACE_CONF`), KHÔNG vẽ điểm nóng giả, lớp "Mọi loài" chỉ tính loài định-vị-được, thẻ cá nói thẳng "đoán theo MÙA VỤ + ĐỘ SÂU". **Hiển thị kiểu PFZ chuẩn (tham khảo OceanFishMap)**: vùng mềm heatmap **mỗi loài MỘT MÀU** (`SPECIES_PROFILES.color` + `SPECIES_META`; Mọi loài = xanh lá), **hồng tâm** ≤8 điểm nóng nhất (≥75 điểm, cách ≥0.7°, chạm-là-tới, tô màu loài đang chọn); **chọn loài** bằng hàng chip có chấm màu — **loài đang vụ Ở VÙNG ĐANG XEM xếp lên đầu** (viền cam). **ƯU TIÊN GẦN MÌNH**: điểm nóng cộng điểm thưởng theo khoảng cách tới chỗ đang xem / cảng nhà / điểm ghim (≤+12, mờ tới 0 ở ~220 km — không bịa cá, chỉ xếp chỗ gần lên trước), thẻ cá có dòng "Điểm cá gần bạn nhất: ~N hải lý hướng X". Chạm điểm → "Chỗ này có khả năng TỐT/khá/vừa cho: [loài]" + **số môi trường tại ô (nước °C · mồi dày/vừa/loãng)** + ngày ảnh; sheet luôn kèm **tuần trăng đêm nay** (`lib/moon.ts`, tính offline — nghề đèn mực/cá cơm). LUÔN "tham khảo, không phải cam kết". Khẩu vị loài từ tài liệu sinh học công khai — KHÔNG đụng API/dữ liệu đối thủ. **TẦNG NHIỆT (thermocline) ĐÃ THÊM (2026-06-11)**: `lib/hycom.ts` kéo nhiệt-độ-theo-tầng HYCOM ESPC-D-V02 (`tds.hycom.org` OPeNDAP, 1/12°, 40 tầng, nowcast trễ ~1 ngày, KHÔNG key) → tính **độ sâu đẳng nhiệt 20°C (D20)** = yếu tố thứ 7 trong habitat, trọng số CHỦ YẾU cho cá ngừ/cá nổi lớn + mực xà đại dương. Tuỳ chọn như SSHA/anomaly — HYCOM fail thì bỏ (wMax cố định nên điểm GIẢM, không tăng). **TẦNG NHIỆT ĐỔI SANG DỊ THƯỜNG KHÔNG GIAN + THAM SỐ THEO LOÀI (2026-07-26)**: `thermoFit` TRƯỚC chấm D20 **tuyệt đối** bằng `trapezoid(d20, 40, 70, 170, 230)`. CHẨN ĐOÁN trên lưới HYCOM THẬT (`scripts/thermofit-diagnose.mjs`, 1454 ô biển VN có D20): D20 min 59,8 · p10 84,4 · p50 112,8 · p90 120,3 · max 129,6 m — **TOÀN BỘ nằm trong dải "tốt" [70,170]** ⇒ `thermoFit` trung bình **0,996**, độ lệch chuẩn KHÔNG GIAN chỉ **0,028**, 98% số ô ≥0,95, không ô nào ≤0,2. Một yếu tố LUÔN BẬT thì không xếp hạng được ô nào hơn ô nào — chỉ nâng điểm đồng loạt. **Ablation trên `buildFishForecast` thật**: bỏ hẳn tầng nhiệt kéo %điểm nóng **49,6% → 19,9% (Δ −29,7đ)**, lớn nhất trong mọi nguồn (anom −2,1 · cur −3,6 · depth −1,6 · sla −21,9) ⇒ chính nó làm bản đồ "nhìn đâu cũng đỏ". Đây là **cùng LOẠI lỗi** đã sửa hai lần trước (`upwTerm` dị thường nhiều năm, `wMax` thiếu nguồn làm điểm tăng). NAY `thermoFit(dịThườngD20, band)` chấm theo **DỊ THƯỜNG KHÔNG GIAN của D20** (`spatialAnomaly`, cùng bán kính 2,5° với upwTerm/coldStrength): nêm nhiệt NHÔ so với vùng lân cận (dome/ridging) = dinh dưỡng đẩy lên + tầng sống bị nén → cá nổi dồn; nêm CHÌM = lớp ấm dày, tầng lặn ngày rộng. **Dải theo LOÀI** (`SpeciesProfile.thermoBand`, tuỳ chọn): luật chung = `[p50, p90]` của |dị thường| TRÊN CHÍNH PHÍA đó (đúng 10% ô mỗi phía đạt 1, cùng tinh thần UPW_SCALE/COLD_SCALE) → mặc định `THERMO_BAND_DEFAULT = [-4, -23]` (phía nông: p50 4,1 m · p90 22,8 m) cho cá nổi mặt (ngừ vây vàng/vằn/chù/ồ/chấm, thu, cờ, nục heo, ngân, mực xà); **cá ngừ MẮT TO `[4, 12]`** (phía sâu: p50 4,0 m · p90 11,7 m) — SỬA LỖI SINH HỌC đội phản biện nêu: trước đây vây vàng (bám lớp trộn/đỉnh nêm) và mắt to (ngày lặn 200–500 m, quanh/dưới nêm) dùng CHUNG một thước dù sinh học ngược nhau. Hai phía KHÔNG đối xứng (đuôi nông tới −47 m, đuôi sâu chỉ +22,5 m) nên mỗi phía đo riêng, không lấy gương. KẾT QUẢ trên API THẬT: %điểm nóng **49,0% → 21,1%**, tầng nhiệt hạ từ "yếu tố gánh −29,7đ" xuống ngang hàng các cơ chế khác (−1,4đ). ĐÁNH ĐỔI đã đo: `ngừ mắt to` 84 → 8 ô ≥50 (vẫn 348 ô trong payload ≥25) vì `w.thermo`=0,5 là trọng số LỚN NHẤT của nó nên mất "nền luôn bật" là mất nhiều nhất — thưa nhưng ĐÚNG; `ngừ vây vàng` 457→217, `mực xà` 328→66, các loài không dùng tầng nhiệt KHÔNG đổi. **NHIỆT ĐÁY cho loài đáy mềm (VIỆC 4, 2026-07-25)**: **11 loài ĐÁY MỀM thềm/cửa sông** — 7 cá đáy mềm (mối/đổng/phèn/khoai/bơn/đù/chim) + 4 giáp xác (tôm bạc/sú/ghẹ/cua) (`SpeciesProfile.tempSource="bottom"`) TRƯỚC chấm cổng nhiệt bằng SST MẶT — nước mặt Biển Đông ~27–30°C nằm trọn dải tối ưu → tFit≈1 khắp nơi → bản đồ cá đáy gần MẢNG TÔ ĐỀU. NAY chấm bằng **nhiệt ĐÁY** (tầng sâu nhất còn hữu hạn của cột HYCOM, `bottomTempGrid`, dùng lại cube D20 — không fetch thêm). Validate trên số HYCOM THẬT (`scripts/fish-predict-viec4-bottom.mjs`): cột NƯỚC SÂU phân tầng rõ (đáy lạnh hơn mặt ~13°C) → loài đáy mềm bị loại ĐÚNG khỏi ô nước sâu, giữ vùng thềm nông ấm; thềm nông mùa đông xáo trộn đều nên đáy≈mặt (đổi ít, đúng vật lý). KHÔNG loài nào trong vụ biến mất (fallback SST mặt khi HYCOM fail/ô NaN); %điểm nóng "Mọi loài" KHÔNG phình (loài đáy là `low`, không tính vào "Mọi loài"). **CÁ RẠN (hồng/mú/kẽm) GIỮ nhiệt MẶT** — KHÔNG dùng nhiệt đáy: rạn sống nông 2–50m, cube HYCOM thô ~53km ở rìa thềm dễ lấy nhầm nhiệt nước sâu lạnh cho ô rạn nông → cú co 80–96% (kẽm 1193→49 ô) là ARTIFACT chứ không phải loại-đúng; cá rạn `low` (bám cấu trúc rạn — dữ liệu ta không có), cắt mạnh bằng nhiệt đáy = ĐỘ CHÍNH XÁC GIẢ, ngược nguyên tắc trung thực → để mặt (mờ, low-confidence, thành thật "chưa định vị được"). **Cá ngừ mắt to GIỮ cổng nhiệt MẶT** (không dùng "deep"): validate thấy nhiệt 250m gần ĐỒNG NHẤT (~13°C, p10–p90 12.5–13.5) → cổng nhiệt-sâu ≈1 mọi nơi, KHÔNG tạo biến thiên lại bỏ phần ghìm của cổng mặt mùa hè → phình điểm nóng; tín hiệu không gian thật của mắt to là ĐỘ SÂU nêm nhiệt (D20, đã có). **Còn lại**: độ mặn model HYCOM s3z (giá trị thấp cho cá nổi VN — để sau). **PHẠM VI THỜI GIAN: BẢN ĐỒ CÁ CHỈ MỘT BẢN — KHÔNG chạy theo ngày (2026-07-26)**. Chủ dự án chốt hướng "bản đồ cá hôm nay → +3 ngày" (sau khi đã bác bỏ 10 ngày bằng số). ĐO TRƯỚC KHI DỰNG (`scripts/fish-3day-probe.mjs`): dựng SST cho D+1..D+3 bằng **neo vệ tinh + xu hướng Copernicus** `sst_pred(D+k) = sst_sat(D) + α_k·[cop_thetao(D+k) − cop_thetao(D)]` (α cross-validated từ `src/data/copernicus-tendency-skill.json`: 0,455 · 0,532 · 0,604; nhiệt là thành phần DUY NHẤT có kỹ năng xu hướng — phù du corrTendency ≈ 0 nên GIỮ ảnh hôm nay, front nhiệt neo vào còn XẤU đi nên chỉ dùng nhiệt cho GIÁ TRỊ, không cho front), rồi chấm lại `buildFishForecast` và so bản đồ. **KẾT QUẢ trên 3 mùa THẬT** (24/7/2026 · 10/1/2026 · 8/4/2026): Jaccard tập ô ≥50 giữa D+0 và D+3 = **0,926 · 0,982 · 0,967**; số ô ĐỔI TRẠNG THÁI điểm nóng chỉ **1,64 % · 0,52 % · 0,53 %**; |Δđiểm| trung bình **0,51 · 0,12 · 0,27** trên thang 100 (p95 = 4 · 1 · 2); %điểm nóng 22,1→20,9 · 28,5→28,2 · 15,9→15,7. Ngưỡng quyết định ĐẶT TRƯỚC khi chạy (J < 0,90 **hoặc** ≥5 % ô đổi trạng thái ở D+3) **không mùa nào chạm**. Lý do vật lý: SST đổi RMS ~0,39 °C sau 3 ngày, hãm theo α còn 0,10–0,24 °C — quá nhỏ so với bề rộng cổng nhiệt loài (vài °C). ⇒ **KHÔNG dựng 4 bản, KHÔNG dựng thanh trượt giả** (cho kéo ngày mà bản đồ y hệt = nói dối bằng hình), Copernicus `thetao` (nhiệt) **KHÔNG vào runtime** (quyết định này CHỈ về lớp nhiệt cho bản đồ 3 ngày; từ 2026-07-26 Copernicus CÓ trong runtime nhưng cho **dòng chảy** `utotal/vtotal`, việc khác — xem "ĐỔI NGUỒN HỘI TỤ" ở trên). Thay vào đó nói THẬT trong sheet khi bà con kéo sang ngày khác: *"Chỗ cá ít đổi trong vài ngày tới — cái đổi là gió, sóng."* (xem 07-design-spec §10.5). Theo nhóm loài, nhạy nhất với nhiệt là **cá rạn** (J 0,687 ở D+3 hè; 6,3 % ô đổi ở tháng 4) và **mực** (J 0,824) — nhưng cả hai đều `surfaceSignal: low`, không tính vào lớp "Mọi loài"; cá nổi lớn gần như bất động (J 0,959–0,988). Muốn mở lại quyết định này thì chạy lại probe, KHÔNG đoán. **MÙA VỤ MỀM + BỎ DEAD FIELD `w.food` (2026-07-27)**: hai dọn dẹp theo chẩn đoán nghiên cứu ngoài (`Documents/Codex/2026-07-25`). (1) **Mùa vụ hết là cổng NHỊ PHÂN**: trước `f.months.includes(month)` → trong vụ ×1, ngoài vụ loại hẳn (điểm NHẢY VÁCH 0↔đầy ở ranh giới tháng, không biểu diễn đầu/cuối vụ). NAY `seasonPrior(months, month)` ∈ [0,1] (`fish-seasons.ts`) làm THỪA SỐ trong `fit`: chính vụ = 1 (KHÔNG đổi bản đồ), tháng đệm liền kề = 0,5 (`SEASON_TAPER_MONTHS=2`, vòng tròn T12↔T1), cách ≥2 tháng = 0. Vạt giữ HẸP có chủ ý — chỉ làm mượt ranh giới, không kéo dài vụ; loài quanh năm (cá ngừ đủ 12 tháng) luôn =1 nên KHÔNG ảnh hưởng loài khơi. Là thừa số ≤1 nên bất biến monotonic giữ nguyên. (2) **`w.food` là dead field** (khai trong hồ sơ loài nhưng KHÔNG bao giờ được đọc — đổi `food:0.8→0` không đổi kết quả) → **bỏ khỏi `w` của 40 loài**, thay bằng `foodDependence?` ∈ [0,1] (mặc định 1) nối THẬT qua `foodGate(food, dependence)`: dep=1 = hành vi cũ (`FOOD_FLOOR+(1−FOOD_FLOOR)·chlFit`), dep=0 = mồi không gồng cổng. Chưa loài nào đặt <1 (giữ nguyên bản đồ) — chỉ nên hạ khi có CPUE hiệu chỉnh. Test: `seasonPrior`/`foodGate` khoá ranh giới 0/1. KHÔNG đụng dữ liệu/nguồn mới.
  - **Cá mùa này** (fallback + ngữ cảnh): `src/data/fish-seasons.ts` — 7 vùng biển × **40 loài** × tháng (mở rộng 2026-06-10, mỗi loài 1+ dòng theo vùng/mùa), tổng hợp từ bản tin RIMF + FAO/FishBase + báo ngành (THAM KHẢO). `species` khớp ĐÚNG `SPECIES_PROFILES.species` (test ép phủ hai chiều). Nâng cấp tương lai: nối bản tin dự báo ngư trường RIMF theo kỳ.
  - **Nước dâng/xoáy (SSHA)**: `src/lib/sea-scalars.ts` + route `/api/sea-scalar` — dị thường mực nước (ERDDAP, tươi hằng ngày) vẽ ô màu 0.5°: nhô = xoáy ấm, lõm = xoáy lạnh gom mồi. **Độ mặn: TẠM RÚT khỏi UI** — SMOS bị nhiễu RFI che trắng Biển Đông, SMAP ngừng 2021–22 (code giữ, có nguồn sống bật lại 1 dòng trong `SEA_SCALAR_ORDER`). **Tầng nhiệt**: nguồn miễn phí (OHC/iso26C) ngừng 2024 — chưa làm, không dùng dữ liệu cũ giả làm mới.
  - Hải đồ: độ sâu đáy biển EMODnet/GEBCO (tĩnh, không đổi theo ngày — UI ghi rõ) + đường đẳng sâu tự sinh (ETOPO) + báo hiệu hàng hải (phao, đèn biển) OpenSeaMap, tự hiện khi zoom gần bờ. Lưu ý OpenSeaMap là dữ liệu cộng đồng, vùng VN còn thưa — chỉ là tham khảo thêm, không thay hải đồ giấy/máy định vị của tàu.
  - **TÊN ĐẢO ĐẦY ĐỦ + TUYẾN HÀNG HẢI (2026-08-07)** — "đúng chất hải đồ", chủ dự án chốt trọn gói. Hai **asset tĩnh cùng-origin** (SW giữ sẵn → mất sóng vẫn có): (a) `public/data/vn-islands.v1.json` — **~103 đảo có tên TIẾNG VIỆT** (ven bờ 42 + Hoàng Sa 36 + Trường Sa 25), sinh bởi `scripts/generate-islands.mjs` từ **Wikipedia tiếng Việt** (toạ độ + tên là dữ kiện, không vướng bản quyền); render lớp `symbol` MapLibre tự giãn theo zoom (rank 1 hiện sớm → rank 3 chỉ khi zoom sâu). (b) `public/data/vn-sea-lanes.v1.json` — tuyến hàng hải lớn **vẽ tay** (5 tuyến, né va chạm, nhãn "tham khảo") + chi tiết hải đồ **kéo từ OpenStreetMap lúc build** (`scripts/generate-sea-lanes.mjs`): **25 luồng cảng · 108 phân luồng · 103 cáp/ống ngầm · 41 giàn khoan · 8 vùng cấm**; bật/tắt trong panel Hải đồ. ⚠️ **CHỦ QUYỀN**: KHÔNG dùng thô OSM/GeoNames ở Hoàng Sa/Trường Sa (hai nguồn đó chỉ có tên chữ Hán / Anh / Philippines — vi phạm điều app cấm); tên đảo 100% tiếng Việt, đơn vị hành chính GÁN CỨNG (Hoàng Sa→TP Đà Nẵng, Trường Sa→tỉnh Khánh Hòa); tuyến/luồng OSM **bỏ hết tag `name`**, chỉ giữ hình học. Cổng test `src/lib/islands.ts` + `__tests__/islands.test.ts` đọc file thật, có MỘT ký tự Hán/CJK là ĐỎ. Nghiên cứu nguồn: [../research/09-nautical-map-sources.md](../research/09-nautical-map-sources.md).
  - Gió/sóng + mưa/dông: Open-Meteo (mô hình quốc tế, cập nhật theo giờ) — UI luôn kèm lời dặn nghe đài duyên hải / biên phòng. Dông sét trừ 30 điểm đi biển và tô đỏ — nguy hiểm nhất với tàu nhỏ.
  - **Tầm dự báo 1–16 ngày** (mở rộng 2026-07-25, `FORECAST_MAX_DAYS = 16` = trần nguồn): gió/mưa/dông từ mô hình best-match Open-Meteo (GFS 16 ngày) và **SÓNG từ NCEP GFS-Wave 0.25° toàn cầu** (`WAVE_MODEL = "ncep_gfswave025"` trong `sea.ts`/`marine-weather.ts`, cũng 16 ngày) — best-match sóng chỉ phủ ~8 ngày nên phải chỉ định model mới đủ 16. Ngày sóng thủng (hiếm) ước thô từ gió (`estimateWaveFromWind`, cờ `waveEstimated`) thay vì để 0 giả êm.
  - **Độ tin nói THẬT theo từng ngày** (`forecastConfidence` mở tới 16 ngày + lớp `lib/forecast-quality.ts`): gộp 3 tín hiệu — (1) tầm ngày (skill khí tượng giảm dần), (2) **ensemble spread** (`lib/forecast-ensemble.ts`, Open-Meteo GFS-EPS 31 thành viên — mô hình lệch nhau nhiều = khó đoán, hạ độ tin kể cả ngày gần), (3) **bảng skill backtest** (`src/data/forecast-skill.json` — dự-báo-cũ vs thực-tế ERA5, sinh offline bởi `scripts/forecast-backtest.mjs`, xem [ops/forecast-accuracy.md](ops/forecast-accuracy.md); đo tới lead 15, ngày 16 lùi về độ tin theo tầm ngày). Bảng skill còn dùng nắn **bias** thô của điểm số (`applyBiasCorrection`) — phần "tối ưu độ chính xác" từ dữ liệu thật. Nhãn nền: 1–3 ngày khá sát · 4–7 tham khảo · 8–10 liệu đường · 11–16 hướng chung, chắc chắn xem lại. Ảnh vệ tinh là ảnh ĐÃ CHỤP — không dự báo trước được, UI nói thẳng cạnh bộ chọn ngày.
  - Dẫn đường tiết kiệm dầu: thuật toán theo mô hình nghiên cứu VISIR cho tàu nhỏ (xem [../research/06-weather-routing.md](../research/06-weather-routing.md)) — lưới phủ vùng + Dijkstra theo giờ dự báo Open-Meteo (72h: sóng + hướng, gió + hướng, **dòng chảy mặt biển** nguồn SMOC ~8 km gồm cả dòng triều — cộng vector vào tốc độ tàu, biết "đi nhờ nước"/né nước ngược), né bờ/rạn/bãi cạn bằng lưới độ sâu 6 lớp đóng gói sẵn (ETOPO 2022 + GEBCO + mặt nạ rạn OSM + đường bờ thật `vn-coast`, ô ~450 m — [04 §7e](04-data-model.md)): **đất chặn ngoài 5 km quanh hai đầu; rạn và nước <2 m chặn ngoài 12 km; dải 2–4 m chỉ mở cho tàu ĐÃ KHAI MỚN khi mớn + 0,5 + ½·sóng ≤ 2,0 m (chưa khai thì chặn — không đoán mớn); 4–12 m đi được có cảnh báo** (đổi 2026-09-04, Đợt 0: bản cũ "chặn <4 m cho mọi tàu" làm Rạch Giá không có tuyến nào ra khơi). **Mô hình dầu là ƯỚC LƯỢNG THAM KHẢO** (sóng làm tàu chậm theo hướng sóng — hệ số Kwon 4 bậc; ngược gió ăn dầu hơn). **An toàn theo thang KTTV VN** (audit 2026-06-10, research/06 §3b): cấp 6/sóng 2–3 m phạt nhẹ "cẩn thận" · cấp 7/3–4 m cảnh báo đỏ "không nên đi" · cấp 8/≥4 m chặn cứng; sóng đuôi ngắn ≥2 m chỉ CẢNH BÁO giảm ga; **trần đường vòng 30%** — đường thẳng đi được mà tuyến vòng quá 30% thì trả đường thẳng + cảnh báo, không vẽ tuyến 4× rồi gọi là tiết kiệm. KHÔNG hứa con số lít chính xác; chưa biết đá ngầm nhỏ, luồng lạch; dòng nước sát bờ kém chính xác (nguồn ghi rõ "not suitable for coastal navigation") — UI bắt buộc dặn dò hải đồ + nghe đài trước khi chạy. **KHO HẢI ĐỒ VÀO THUẬT TOÁN (2026-09-04, Đợt 1–2)**: tuyến nay CHẶN THẬT theo dữ liệu hàng hải trong máy — xác tàu/chướng ngại/vật chìm (Thông báo hàng hải, vòng tránh ≥500 m, ×2 khi tin "theo phao" hoặc quá 3 năm), giàn khoan 1 km, phao báo chỗ nguy hiểm cô lập 300 m, lồng bè 200 m, vùng CẤM VÀO (đa giác); đường chim bay dùng để so sánh cũng đi qua đúng luật đó nên không còn ca "đi thẳng xuyên xác tàu". Trong 5 km quanh bến thì KHÔNG chặn mà chỉ nói ra (cảng nào cũng có lồng bè, đăng đáy — chặn là không tàu nào rời bến được). Cáp ngầm/ống dẫn, khu cấm neo/cấm đánh bắt, số đo sâu chính thức + con nước tại giờ tàu tới, độ sâu luồng, phao tiêu dọc đường và ranh giới biển thì KHÔNG chặn — chúng thành CÂU CHỮ trên thẻ tuyến (hậu kiểm), vì đó là chuyện "biết mà tránh/chờ", không phải chuyện vẽ đường vòng. Kho nào không tải được thì thẻ tuyến GỌI TÊN kho đó, không im như đã đối chiếu xong.
    - **Dẫn đường LIVE "Bắt đầu dẫn đường" (2026-07-28)**: vẽ xong tuyến → theo dõi GPS trên máy, **bám tuyến đã vẽ** (dẫn tới khúc rẽ kế tiếp), tính quãng + giờ còn lại tới đích, giữ màn hình sáng. Chạy hoàn toàn TRÊN MÁY, **không cần mạng** (khớp offline). **Vị trí CHỈ nằm trên máy — KHÔNG gửi đi đâu, không log**; chia sẻ vị trí cho người nhà (VMS) là việc TƯƠNG LAI, ngoài phạm vi bản này. Vẫn "chỉ tham khảo — không thay máy định vị của tàu"; mất định vị/tàu chưa chạy thì nói thật, không bịa số. Xem [07-design-spec §10.7](07-design-spec.md).
    - **Đường đi NHIỀU ĐIỂM (2026-08-28)**: bà con chấm một chuỗi chỗ ghé (tối đa 6) và **tàu đi ĐÚNG THỨ TỰ đã chấm** — app KHÔNG tự sắp xếp lại, vì thứ tự là kinh nghiệm thuyền trưởng (tối ưu thứ tự thuộc tính năng lộ trình nhiều ngày, [09 §6](09-ba-spec-lo-trinh-chuyen-bien.md)). Mỗi chặng vẫn tính bằng đúng thuật toán né sóng/cạn trên, giờ xuất phát chặng sau cộng dồn giờ chạy chặng trước; ba con số (hải lý · giờ · lít dầu) là của CẢ đường đi và **mọi cảnh báo của từng chặng đều nổi lên cả tuyến** (một chặng cạn = cả tuyến nói cạn). Chuỗi chỗ ghé lưu trong máy (`forfish.routestops.v1`) nên tắt app không mất. **Cảnh báo lệch tuyến** nay nói theo mốc (2/5/10/20 km) kèm chuông riêng — vẫn chỉ CẢNH BÁO, app không tự tính lại tuyến và không phán đúng-sai. Vẫn MIỄN PHÍ như phần dẫn đường.
  - Tin bão/áp thấp: **HAI NGUỒN (2026-08-18)** — (1) **NCHMF** (Trung tâm Dự báo KTTV quốc gia), chính là bản tin bà con nghe trên đài duyên hải: parse trang bản tin `nchmf.gov.vn/kttv/` (`lib/storms-vn.ts`, thuần + test dựng từ bản tin thật), **ưu tiên** vì bà con đối chiếu với đài; (2) hệ cảnh báo thiên tai quốc tế GDACS (EU/UN, JSON công khai) qua proxy `/api/storms`, lọc vùng Biển Đông, bù đường đi + vùng ảnh hưởng dạng polygon. **VÌ SAO PHẢI CÓ NGUỒN VN**: 18/8/2026 người của SDVICO báo từ hiện trường *"đài dự báo áp thấp nhiệt đới trên Biển Đông mà app chưa cập nhật"* — GDACS **không phủ áp thấp nhiệt đới mới hình thành** (đo thật hôm đó: 0 sự kiện trong khung Biển Đông trong khi NCHMF đã ra tin). Hỏng một nguồn vẫn còn nguồn kia; cả hai hỏng thì nói "chưa hỏi được", KHÔNG BAO GIỜ nói "không có bão". **Vẽ trên bản đồ (2026-06-16)**: ngoài tâm bão còn có **đường đi (track)** + **vùng ảnh hưởng (polygon đỏ mờ)** — GDACS trả sẵn LineString/Polygon, parse trong `storms.ts`. **Quy tắc an toàn** (sửa sau audit 2026-06-10): nguồn fail → hiện rõ "Chưa kiểm tra được tin bão — nghe đài duyên hải" (KHÔNG BAO GIỜ nói "không có bão" khi chưa chắc; cũng không im lặng — người dùng không phân biệt được im lặng với "đã kiểm tra, không có"); dòng trấn an xanh chỉ hiện khi đã kiểm tra được thật. Nguồn quốc tế có thể lệch tên/cấp so với bản tin KTTV VN bà con nghe đài — nâng cấp lên nguồn chính thống VN khi có thỏa thuận.
- **Số đo sâu chính thức (2026-09-01)**: bóc từ **Thông báo hàng hải** — `vmsa.vn`, kho `vms-south.vn`, 16 trang cảng vụ tỉnh → **394 điểm · 313 tuyến/khu nước** + **22 đoạn luồng** khống chế. Mở bằng **OCR 987 PDF ảnh scan** (EasyOCR `vi`+`en`, venv ngoài repo, `package.json` không đổi một dòng): miền Bắc thêm 107 tuyến từ 16,9°B ra Bắc (sáu cảng vụ trước đây đóng góp **0**), miền Nam thêm **15 điểm rời + 217 tuyến**. Vĩ độ tuyến nay phủ **8,7 → 21,4°B**. Đây là **dữ liệu nhà nước công bố công khai**: công ty hiện thực hoá cho bà con xem trực quan.
  - Hai bộ **BÙ NHAU chứ không chồng**: miền Nam ghi theo TOẠ ĐỘ RỜI, miền Bắc ghi theo ĐOẠN GIỮA HAI PHAO. Bỏ một bên là mất trắng một nửa đất nước.
  - ⚠️ Là dữ liệu **CỬA LUỒNG/CẢNG**, không phải ngư trường xa bờ — nhãn UI nói thẳng *"tham khảo, không thay hải đồ chính thức"*. Ngư trường xa bờ và cửa lạch/cảng cá vẫn TRẮNG, chưa hứa gì.
  - **Miền Bắc thêm 0 điểm rời — đó là kết quả ĐÚNG.** Cảng vụ phía Bắc không đăng bảng toạ độ điểm cạn; họ đăng góc khu nước kèm một câu độ sâu khống chế. Gắn số đó vào bốn góc là **bịa bốn phép đo ở toạ độ thật**, nên chúng nằm ở `tuyen[]`/`doan[]`.
  - **Ngày ước lượng NAY CÓ CỜ**: 149 điểm mang ngày suy từ `/uploads/<năm>/<tháng>/` — trước đây trình bày như ngày ký chính xác. Giao diện đọc cờ này để làm THÔ câu trả lời ("khoảng 7 năm trước", không nói tháng) và nói rõ ngày là ước chừng.
  - **Kho ảnh scan nay đã OCR hết.** Còn lại **14 PDF hỏng cấu trúc** và **55 bản tải lỗi** — phát hiện đáng nói: 55 bản đó **chưa bao giờ là PDF**, chúng là trang HTML lỗi của kho lưu trữ mà bộ bóc cũ dồn nhầm vào sọt "ảnh scan"; ai đọc `boSot` sẽ đi OCR một trang HTML. Nay ghi đúng lý do *"cần tải lại, không phải OCR"*.
  - ⚠️ **Điểm rời từ 12°B ra Bắc vẫn là 0** — không phải thiếu sót của OCR mà là khuôn xuất bản: cảng vụ phía Bắc không đăng bảng toạ độ điểm cạn. Muốn có phải xin bản khảo sát gốc.
  - Đối chiếu chéo với lưới vệ tinh ETOPO+GEBCO: **khớp 99%** (2/379 điểm lệch, đều ở mép ngưỡng; 0/13 đoạn luồng lệch). Vệ tinh nay dùng để **soi lỗi bóc**, không dùng để sửa lưới.
- **Chất đáy · địa danh ngầm · khu neo đậu tránh trú bão (2026-09-03) — ĐỀU MIỄN PHÍ mọi tài khoản** (bản đồ cho không; chỉ dự báo cá + thời tiết >3 ngày mới premium — [tier.ts](../../src/lib/tier.ts)):
  - **Chất đáy** — 362.743 điểm phân loại đáy biển (Cát/Đá/San hô/Vụn/Cỏ biển/Thảm rong) từ **Allen Coral Atlas** (`benthic_data_verbose`, CC BY 4.0). Trả lời câu ngư dân hỏi trước khi thả neo: đáy đá/san hô = neo khó bám; cá đáy theo chất đáy. Vẽ bằng vector tile pmtiles (8,1 MB), chạm ra "Đáy: San hô · neo khó bám". Công tắc "Chất đáy" (mặc định bật). GIỚI HẠN nói thật: ACA chỉ khảo sát vùng rạn/đảo nước trong — không phủ đáy bùn cửa sông (chỗ trống = chưa khảo sát, không phải đáy sạch).
  - **Tên địa danh ngầm** — 184 tên thực thể ngầm (núi/hố/thung lũng/guyot/sống/vách ngầm), tên tiếng Việt, từ **Thông tư 33/2024/TT-BTNMT Mục III** (toạ độ VN-2000). Công tắc "Tên địa danh ngầm" (mặc định bật).
  - **Khu neo đậu tránh trú bão** — 51 khu (20 vùng + 31 khu cấp tỉnh, phủ 21/28 tỉnh ven biển) từ **QĐ 582/QĐ-TTg (2024)**. Chấm xanh "bến an toàn", chạm ra tên + sức chứa + độ tin toạ độ. Đúng thứ cần khi bão tới. Toạ độ đối chiếu từ đèn biển + phao luồng, KHÔNG geocode tự do. Công tắc "Khu tránh trú bão" (mặc định bật). ⚠️ 5 khu còn dùng toạ độ OSM (ODbL) — cờ trong dữ liệu, thay nguồn trước khi vào gói bán.
  - Cả ba là **thông tin nhà nước/khảo sát công bố công khai** (ACA giấy phép mở CC BY) — công ty hiện thực hoá cho bà con xem trực quan là việc bình thường. Đều nằm trong service worker nên xem được khi mất sóng. Kiến trúc + schema: [02-architecture.md](02-architecture.md) 2026-09-03, [04-data-model.md](04-data-model.md) §7c.
- **Lấp lỗ hải đồ vòng 2 (2026-09-03) — CŨNG MIỄN PHÍ mọi tài khoản**:
  - **Tên rạn/bãi/đá đầy thêm** — lớp "Đá ngầm · Rạn" từ 13 lên **1.374 tên tiếng Việt** theo **Thông tư 33/2024/TT-BTNMT** (ven bờ 1.225 · Trường Sa 106 · Hoàng Sa 28 · thềm lục địa/DK1 15). Cộng 184 địa danh ngầm ⇒ **~1.558 tên Việt** trên hải đồ, tránh sạch tên Trung/Hán. Ven bờ dày nên chỉ hiện khi zoom sát; ngoài khơi (mốc chủ quyền) luôn hiện. Nguồn kiến trúc: [02-architecture.md](02-architecture.md), số: [04-data-model.md](04-data-model.md) §7d.
  - **Con nước (thuỷ triều)** — engine tính trong máy, nay **11 trạm** = 4 trạm ĐO (UHSLC) + 7 trạm MÔ HÌNH **EOT20** (DGFI-TUM, SEANOE **CC-BY 4.0** — thương mại được), phủ thêm **bờ Tây (Rạch Giá/Cà Mau — vịnh Thái Lan)** trước đây trắng. Trạm mô hình **luôn báo "ước tính"** (kể cả khi ở gần) — không đội bản mô hình lên bằng bản đo, vì con nước dính chuyện qua lạch an toàn.
  - **Cáp quang biển VN** — thêm 10 tuyến cáp (điểm cập bờ Vũng Tàu/Đà Nẵng/Quy Nhơn) vào lớp tuyến tàu để bà con biết chỗ có cáp mà tránh thả neo/giã cào làm đứt. Nguồn: **TeleGeography (CC BY-NC-SA) chỉ dùng làm MANH MỐI cho điểm cập bờ — KHÔNG copy hình tuyến**, cộng tin công khai VN, đối chiếu ≥2 nguồn.
- **"Hải đồ chi tiết" dựng lại theo chuẩn hải đồ (2026-09-03b) — MIỄN PHÍ mọi tài khoản.** Thuê góc nhìn reviewer hải đồ (IHO INT1/IALA, quen C-MAP & Navionics) chấm lớp này — [research/danh-gia-hai-do-2026-09.md](../research/danh-gia-hai-do-2026-09.md): 6 hạng mục "MỘT PHẦN"/"CHƯA ĐẠT" đều đã sửa. Những gì bà con thấy khác:
  - **Đá, bãi, rạn là ba HÌNH khác nhau** (đá = sao gai đỏ "đâm là thủng", bãi/cồn = "có thể ngập", rạn = răng cưa) thay vì một chấm teal — nhìn là biết, không phải đọc chữ đầu tên. Mọi hình tự vẽ, không sao chép bộ ký hiệu nào.
  - **Cáp quang ≠ ống dẫn ≠ vùng cấm neo**: ba nét khác cả màu lẫn độ đậm, vùng cấm có nền + tên + hình mỏ neo gạch chéo; chạm vào ra thẻ *"đừng thả neo, đừng giã cào"*. Lý do an toàn: kéo lưới trúng cáp là bồi thường, trúng ống khí là cháy nổ. ⚠️ Nói thật: ống dẫn hiện có đều ở HK/Quảng Đông — ống dầu khí VN (Bạch Hổ–Nam Côn Sơn) chưa có nguồn mở, cần PVN sau.
  - **Dải độ sâu 4 nấc + đường an toàn 10 m vẽ đậm** (thứ C-MAP có mà app chưa): nước càng sâu càng sáng, và một đường đậm là ranh "dưới đó phải nhìn con nước" cho tàu mớn 1,8–2,5 m.
  - **Chú giải "Ký hiệu là gì?"** ngay trong panel lớp — hiện đúng hình đang vẽ, offline vẫn có; bà con không cần thuộc ký hiệu hải đồ giấy.
  - **Chạm hết hụt**: vùng chạm đệm 56 px, lấy vật gần ngón tay nhất; tên rạn/bãi/đá và cáp/ống/giàn/vùng cấm nay đều chạm được. Thẻ phao thêm câu *"Vào luồng: để phao này bên TRÁI tàu…"*.
  - Mọi lớp nét lên tương phản ≥3:1 dưới nắng; zoom xa vẫn sạch (cáp/luồng/khu trú bão chỉ hiện khi phóng tới mức vùng). Vẫn *"tham khảo, không thay hải đồ chính thức"*. Kiến trúc: [02-architecture.md](02-architecture.md) entry (19); màn hình: [07-design-spec.md](07-design-spec.md) §A; số: [04-data-model.md](04-data-model.md) §7d.
- **Dữ liệu tương lai**: feed thương mại (vd OceanByte) — **bắt buộc đi qua adapter có thể thay thế**.
  - ⚠️ OceanByte là bên thứ ba nước ngoài, có sản phẩm vessel-tracking cạnh tranh → **không bao giờ là core**, không hardcode vào domain logic.
  - ⚠️ Khuyến nghị ngư trường của họ chỉ cập nhật **2 lần/tuần** → KHÔNG hứa với người dùng độ chính xác hằng ngày cho phần khuyến nghị.
- ⚠️ Độ phân giải ảnh vệ tinh là **mức vùng (vài km)**, không phải tọa độ điểm — không hứa "chỉ đúng chỗ thả lưới". Lớp phù du bị mây che mất chỗ — UI giải thích "chỗ trống là mây che".

### Trục 2 — Bán được đắt hơn (ở `/tien`, dock nhãn "Giao dịch" · route cũ `/gia-ca` → redirect)
- **Hứa gì**: cá về bờ bán được giá, không bị ép.
- **Cấu trúc GỌN VỀ 1 VIỆC MUA–BÁN (user chốt 2026-07-27)** — bỏ tab Hiệu quả/Công nợ khỏi khu này, chỉ còn 3 mục:
  1. **Giá cá** — giá cá tuần VASEP + giá dầu DO live. **Chạm 1 loài → biểu đồ giá LỊCH SỬ** (kiểu chứng khoán, 2026-07-29).
  2. **Tin mua/bán** — chủ tàu tự ĐĂNG tin bán (có cá cần bán) và tin mua (cần mua gì); đầu nậu/vựa/nhà máy đăng tin cần mua. Cả làng cùng xem tin đang mở, gọi thẳng nhau.
  3. **Bán ở đâu** — danh bạ đầu mối (nậu vựa · chợ · nhà máy · mối quen), vào thẳng danh sách (bỏ mục "Kênh bán" giải thích).
- **Lộ trình "Tin mua/bán"**: chủ tàu đăng tin ghi thật vào bảng Supabase `market_listings` (RLS owner-write, xem [04 §2](04-data-model.md)); **app riêng cho bên thu mua** sẽ đổ tin cần mua về cùng bảng qua webhook (`owner_id` NULL, `sdwork_ref`). Chưa đăng nhập / chưa cấu hình env → chỉ xem `DEMO_LISTINGS` TIN MẪU (UI ghi rõ) — KHÔNG bịa tin thật.
- **Giá cá LIVE (2026-06-10)**: `/api/port-prices` (cache 24h) kéo bản tin giá nguyên liệu **hằng TUẦN của VASEP** (Khánh Hòa — "giá tại bến/vựa", scrape bảng HTML qua `lib/port-price-source.ts`, khớp keyword → 13 loài app). TRUNG THỰC: giá TUẦN không phải ngày (VN chưa có nguồn giá-tại-bến hằng ngày máy-đọc-được); parser phòng thủ, nguồn fail/parse vỡ → lùi về bảng tĩnh `data/port-prices.ts`; loài tuần này VASEP không có thì giữ giá tĩnh + nhãn "tham khảo"; UI ghi "Nguồn VASEP, tuần X". Loại hàng khô/giống khỏi giá tươi.
- **Biểu đồ giá lịch sử (2026-07-29)**: chạm 1 dòng ở Bảng giá → `PriceHistorySheet` vẽ DẢI giá thấp–cao theo TUẦN (kiểu chứng khoán). Nguồn = `/api/port-prices/history`, gom KHO bản tin VASEP cũ (`?trang=1..2` ≈ 13 tuần ≈ 3 tháng), ngày lấy từ slug URL, parse bằng đúng parser giá tuần. TRUNG THỰC (trục "bán được đắt hơn"): **mỗi điểm là giá VASEP THẬT, KHÔNG nội suy/bịa** — tuần parse hỏng thì bỏ (thành khoảng trống), <2 điểm hoặc nguồn fail → sheet báo "chưa lấy được lịch sử" chứ không vẽ đường giả. Lịch sử tải LƯỜI 1 lần khi chạm thẻ đầu.
- **TÍCH LUỸ lâu dài (2026-07-29)**: kho VASEP chỉ giữ ~13 tuần trên listing → cron `/api/cron/snapshot-prices` (**Vercel cron, thứ Bảy**) UPSERT giá tuần vào bảng `price_history` (Supabase, migration 0016 — xem [04 §2](04-data-model.md)); route ĐỌC DB trước, lịch sử **dài dần theo thời gian** kể cả khi tuần cũ rơi khỏi listing. ✅ migration 0016 ĐÃ apply prod 2026-07-29 (bảng rỗng, backfill ở lần cron chạy đầu — ⚠️ cron thứ 3 cần Vercel Pro, Hobby trần 2). Chưa chạy cron → route tự lùi về gom kho VASEP trực tiếp (biểu đồ vẫn chạy, tối đa ~13 tuần).
- **Giá dầu DO LIVE**: `/api/fuel-price` (cache 6h) → giaxanghomnay.com (Petrolimex, JSON không key) lấy DO 0,05S vùng 1/vùng 2 — chi phí lớn nhất chuyến biển, hiện trên đầu bảng giá. Fail → ẩn (không bịa). `lib/fuel-price.ts` có test.
- **Dữ liệu nậu vựa/người mua**: **tự thu thập** qua mạng lưới đại lý/cảng của SDVICO (moat riêng), feed từ SDWork. Tin cần mua từ đầu nậu/nhà máy còn là tin mẫu chờ app thu mua nối vào `market_listings`.

### Trục 3 — Vận hành rẻ hơn (ở `/tau` tab Dịch vụ/Sản phẩm · route cũ `/van-hanh` → redirect)
- **Hứa gì**: giữ tàu chạy bền, tốn ít tiền hơn.
- **Gồm**: chợ vật tư in-app (dầu nhớt, lọc...), nhắc bảo dưỡng, yêu cầu bảo hành.
- **Dữ liệu/flow**: đơn hàng chảy vào **SDWork** (ERP công ty), thanh toán QR.
- **Đồng bộ ngược từ SDWork (2026-06-10, user chốt)**: khách mua hàng thì SDWork tạo account + đơn + dịch vụ nhưng **không cấp quyền vào SDWork** (app nội bộ/CTV/đại lý) — tài khoản đó tách thành tài khoản ForFish. ForFish tự kéo về (qua adapter, đọc-chỉ): **sản phẩm đã mua + hạn bảo hành, dịch vụ đang dùng + kỳ tới (bảo trì/cước), khoản chờ thanh toán** để nhắc bà con. Chi tiết chuỗi nối: [04-data-model.md](04-data-model.md) §6.
- **ForFish = kênh CSKH của SDVICO (2026-06-10, user chốt)**: tab Sản phẩm hiện danh mục hàng đang bán theo NHÓM (lọc nước biển / giám sát hành trình / wifi biển / lọc dầu / nhớt / sơn tàu / điện-lái) — nhóm ĐÃ MUA gắn nhãn "đang dùng", nhóm CHƯA MUA là gợi ý kèm nút **"Hỏi mua / tư vấn"**; tab Bảo dưỡng đổi thành **Dịch vụ** (sửa chữa + bảo dưỡng + cước + sổ nhắc tự ghi). Mọi nút "Gọi SDVICO" gửi yêu cầu thẳng vào hộp tư vấn của SDWork (kể cả khách chưa đăng nhập = mối bán hàng mới) — nhân viên gọi lại. Đây là vòng lặp cross-trục mục 4 chạy bằng dữ liệu thật.
- **DANH MỤC ADMIN QUẢN LÝ (2026-07-28, user chốt)**: khu "Cửa hàng" của tab Sản phẩm không còn là mảng cứng trong code — admin (`/quan-tri` tab Sản phẩm) ẩn/hiện/xóa/thêm trực tiếp, **áp dụng ngay cho app, không cần build/deploy lại**. Mở rộng thành sàn thông tin sản phẩm đơn giản: cho phép thêm cả sản phẩm/dịch vụ của **đơn vị NGOÀI SDWork** (tên đơn vị + SĐT/ghi chú liên hệ riêng — thẻ hiện liên hệ trực tiếp, KHÔNG đi qua hộp tư vấn CRM của SDVICO vì sai kênh). Bảng `product_listings` ([04 §2](04-data-model.md)), ✅ đã apply prod 2026-07-28. **Lộ trình (thứ tự user chốt, ĐỦ 3 phần)**: (1) danh mục sản phẩm — ĐÃ LÀM; (2) quản lý yêu cầu hỏi mua/tư vấn — ĐÃ LÀM (bảng `product_inquiries` riêng của SDFish, ✅ đã apply prod 2026-07-28; sản phẩm SDVICO vẫn đi CRM `consultation_requests` như cũ, bảng mới chỉ phục vụ sản phẩm đơn vị ngoài — trước đây không nơi nào ghi lại yêu cầu của bà con); (3) **thông báo cho user (Web Push qua PWA, per-user hoặc broadcast) — ĐÃ LÀM** (bảng `push_subscriptions`, ✅ đã apply prod 2026-07-28 — bà con tự bấm "Bật thông báo" ở sheet Tài khoản, admin gửi ở `/quan-tri` tab Thông báo, không cần app store update; ⚠️ còn cần set env VAPID trên Vercel để chạy thật, xem 04-data-model.md).
- **ĐẶT HÀNG TRONG APP (2026-08-11, user chốt)**: khu "Cửa hàng" (tab Sản phẩm) nâng từ "hỏi mua/gọi lại" thành **chợ đặt hàng thật cho một chuyến biển** — chủ tàu xem giá số, chọn số lượng, bỏ giỏ, đặt đơn; **nhà cung cấp (1 NCC — MVP)** nhận đơn và giao, xử lý ở `/quan-tri` tab **Đơn hàng**. Danh mục **tách 3 nhóm: điện tử · cơ điện · nhu yếu phẩm**; cả sản phẩm lẫn nhu yếu phẩm đều đặt được (món chưa niêm yết giá số giữ luồng hỏi mua cũ). **KHÔNG thanh toán trong app** (chốt tiền lúc giao/COD). **Online-only** (không đặt được khi mất sóng — báo trung thực "cần có mạng"). Bảng `catalog_orders` + mở rộng `product_listings` ([04](04-data-model.md), migration 0032/0033 — 🔴 chờ apply). Chủ tàu xem đơn ở mục "Đơn của tôi"; đổi trạng thái → app báo push. Chạy được trên iOS/Android native (mọi call qua `apiUrl`/`authedFetch`).
- **Vai trò**: đây là **động cơ doanh thu** của công ty.

### Trục 4 — Tuân thủ dễ hơn (ở `/tau` tab Giấy tờ + `/nguoi` · route cũ `/giay-to` → redirect · bỏ tab Mức phạt + Checklist xuất bến 2026-07-27, user không cần)
- **Hứa gì**: lo giấy tờ nhẹ đầu, tránh bị phạt oan.
- **Gồm**: tủ giấy tờ (document vault) + nhắc hạn, trợ lý hỏi đáp pháp luật thủy sản VN, **sổ thuyền viên** (`/nguoi`) — định danh **CCCD** (bắt buộc) + bảo hiểm/chứng chỉ hạn.
- **Cảnh báo thuyền viên chéo (premium, 2026-07-27)**: CCCD là định danh toàn hệ thống. Chủ tàu premium báo cáo vấn đề thuyền viên sau chuyến; chủ tàu khác nhập CCCD trước khi thuê thấy cảnh báo **đã được SDVICO kiểm duyệt**. Chống lạm dụng: **kiểm duyệt bắt buộc** (report `pending` im lặng tới khi duyệt) + người bị ghi được phản hồi (qua admin, v1) + khoá tra là HASH(CCCD) (không dò danh sách) + người báo ẩn với người tra. Pháp lý: CCCD là dữ liệu cá nhân — theo tinh thần NĐ 13/2023. Bảng `crew_reports` ([04 §2](04-data-model.md)), 🔴 chưa apply prod.
- **Dữ liệu**: tủ giấy tờ + sổ thuyền viên KHÔNG phụ thuộc nguồn ngoài → được build **ĐẦU TIÊN**. Cảnh báo chéo cần Supabase (server chia sẻ). Logic ở [04-data-model.md](04-data-model.md).
- **Đã gỡ 2026-07-27**: phần TIỀN khỏi Bạn thuyền (sổ ứng + số phần ăn chia) và **máy Chia tiền chuyến** — sổ thuyền viên chỉ còn định danh + giấy tờ + cảnh báo.
- **GIA HẠN GIÁM SÁT HÀNH TRÌNH — VMS (2026-08-21, user chốt)**: thiết bị giám sát hành trình bắt buộc theo luật thủy sản (hết hạn = không được ra khơi/bị phạt) → thuộc trục Tuân thủ. Ngư dân **tự gửi yêu cầu gia hạn** từ SDFish (`/tau` tab **Dịch vụ**, thẻ VMS đặt ngay sau sổ nhắc bảo dưỡng — Lời hứa ≠ tab: đây là promise Tuân thủ nhưng đặt ở khu Dịch vụ cho gần việc "cước/dịch vụ cho tàu") — chọn tàu (từ tàu TỰ THÊM trong máy, phải có mã đăng ký), chọn kỳ hạn **3/6/12 tháng**, nhận **QR VietQR**, chuyển khoản, theo dõi trạng thái. Yêu cầu vào ĐÚNG **pipeline gia hạn S-Tracking dùng chung** của crm-sdvico-40 (DB `exueouggmbjtjvsvpfya`, `origin='sdfish'`); nhân viên đối soát + duyệt → trigger đổ sang trang **"Quản lý gia hạn"** của cskh-tasker-hub. **KHÔNG thanh toán tự động trong app** (bà con chủ động CK). Data vendor: đi qua edge fn `sdfish-renewal` + adapter `src/lib/renewal.ts` — KHÔNG hardcode vào core. Contract: [docs/contracts/stracking-renewal.contract.md](../contracts/stracking-renewal.contract.md). ⚠️ Tàu tự khai → nhân viên xác minh mã tàu trước khi gia hạn. Online-only (báo trung thực "cần có mạng").

## 3. Thứ tự build / Build order

```
Trục 4 + 3  →  Trục 1  →  Trục 2
(không phụ      (feed       (mạng lưới
 thuộc ngoài)    ngoài)      tự thu thập)
```

Hiện trạng (2026-06-10): cả 4 trục đều có MVP chạy được — Trục 1: điểm đi biển dữ liệu thật Open-Meteo + bản đồ ngư trường ảnh vệ tinh (nhiệt độ/phù du/ảnh mây, nhãn chủ quyền VN, chạm xem gió sóng); Trục 2: bảng giá tham khảo + chợ tin mua/bán (sổ lãi/lỗ đã bỏ 2026-07-27); Trục 3: nhắc bảo dưỡng + danh mục vật tư; Trục 4: tủ giấy tờ + tra mức phạt NĐ 38/2024. Dữ liệu giá/vật tư/mức phạt là bản tổng hợp THAM KHẢO từ nguồn công khai — bước tiếp: thay bằng nguồn tự thu qua mạng đại lý.

## 4. Vòng lặp cross-trục / Cross-pillar loop

```
Trục 1 + 4 (điểm biển, nhắc hạn) → mở app hằng ngày
        ↓
  thấy Trục 3 (chợ vật tư) → doanh thu SDVICO
        ↓
Trục 2 tăng thu nhập → mua nhiều hơn từ Trục 3
        ↓
  mọi lượt dùng → làm giàu hồ sơ tàu (boat profile) trong SDWork
```

## 5. Quy tắc adapter / Adapter rule (invariant)

1. Nguồn dữ liệu ngoài (OceanByte, SDWork, kho văn bản luật) chỉ là **phương tiện** — code phải tách qua adapter layer để thay vendor mà không đổi domain logic.
2. Không để tên vendor lọt vào UI copy hay domain types.
3. Không hứa với người dùng điều mà nguồn dữ liệu không đảm bảo (tần suất, độ chính xác).

## 7. Lộ trình: từ 4 lời hứa → app quản lý toàn bộ tàu cá

Định vị mở rộng (2026-06): bốn lời hứa vẫn là **ngôn ngữ giá trị**, nhưng object model của app chuyển sang **CON TÀU** (tàu → chuyến biển → thuyền viên → tiền → giấy tờ). Căn cứ JTBD + chân dung người dùng + mốc pháp lý: xem [06-jtbd-quan-ly-tau.md](06-jtbd-quan-ly-tau.md).

> **Quyết định phạm vi (2026-06-10, từ user)**: khai báo **eCDT / nhật ký khai thác điện tử (NKKT)** là nghiệp vụ với hệ thống NHÀ NƯỚC — **ngoài phạm vi ForFish** (hệ sinh thái đã có sản phẩm NKKT riêng phụ trách mảng này). ForFish chỉ dừng ở: NHẮC mốc nghĩa vụ, checklist trước chuyến, giải thích quy định bằng lời thường. KHÔNG xây wizard khai hộ, KHÔNG tích hợp/đồng bộ hệ thống khai báo nhà nước.

1. **Đợt 1 — Thuyền viên (wedge)** *(đang xây)*: crew module `/nguoi` (hồ sơ + **định danh CCCD** + chứng chỉ/bảo hiểm + **cảnh báo chéo premium**). *(Toàn bộ nhóm "sổ tiền tự ghi" — sổ lãi/lỗ trên nền trip-log, sổ ứng, máy chia tiền, công nợ — đã XÓA HẲN 2026-07-27 theo user: /tien gọn về đúng việc mua–bán, không ôm kế toán chuyến.)*
2. **Đợt 2 — Checklist xuất bến + nhắc tuân thủ**: checklist xuất bến tự sinh theo Lmax (đèn xanh-đỏ "đủ điều kiện xuất bến": giấy tàu, chứng chỉ, bảo hiểm thuyền viên, sổ danh bạ) + cảnh báo hạn giấy tờ cho cả người nhà trên bờ + NHẮC mốc nghĩa vụ khai báo (không khai hộ — xem quyết định phạm vi ở trên).
3. **Đợt 3 — Chợ tin mua/bán nối mạng thu mua**: `market_listings` đã có (chủ tàu đăng tin bán/mua); bước tiếp nối app bên thu mua đổ tin cần mua qua webhook + gói hồ sơ chuyến biển PDF/QR truy xuất cho người mua (làm lại khi cần — trip-dossier cũ đã bỏ). *(Sổ công nợ nậu bỏ khỏi lộ trình 2026-07-27.)*
4. **Đợt 4 — Kết nối SDWork/marketplace**: hồ sơ kinh nghiệm thuyền viên thành "chợ lao động đi biển", kênh bán/chào giá nối mạng đại lý SDVICO, đơn vật tư chảy sâu hơn vào SDWork — chỉ sau khi dữ liệu từ đợt 1–3 đủ dày.

## 8. Cross-references

- Kiến trúc routes/components: [02-architecture.md](02-architecture.md)
- Thiết kế cho ngư dân: [03-design-system.md](03-design-system.md)
- Schema + logic Trục 4: [04-data-model.md](04-data-model.md)
- Cách team agent chia việc: [05-agents-team.md](05-agents-team.md)
- JTBD + map nhóm việc → module (quản lý toàn bộ tàu cá): [06-jtbd-quan-ly-tau.md](06-jtbd-quan-ly-tau.md)

---

**Last updated**: 2026-06-10

<!-- re-verified: 2026-07-26c — RÀ SOÁT MÔ HÌNH (3 việc):
(1) **CỔNG KIỂM ĐỘ PHÂN BIỆT** `src/lib/discrimination.ts` (+ test) — biến "yếu tố có phân biệt được ô nào hơn ô nào" thành đại lượng ĐO ĐƯỢC và CHẶN ĐƯỢC. Lý do: trong MỘT ngày dính BA lần cùng khuôn lỗi "dải khai RỘNG HƠN phân bố cần phân biệt" (`upwTerm` std 0.068 · `thermoFit` 0.028 · `foodLimiter` 0.019), mỗi lần mất một mạch điều tra mới tìm ra. Ngưỡng: std < 0.10 · >90% ô kịch trần · >90% ô kẹp sàn → BÁO ĐỎ. Chạy trên dữ liệu THẬT: `npx tsx scripts/model-discrimination-audit.mjs` (in bảng, thoát ≠0 khi có đỏ; phủ cả 8 cơ chế soft-OR VÀ cổng nhân theo TỪNG loài). Kết quả lần đầu chạy: 19 cổng theo loài báo đỏ (13 `food:*`, 6 `tFit:*`) — `food:mực xà` 0.019/99% trần, `tFit:ngừ chù` 0.027/99%.
(2) **`conv` GỘP KHỐI thay lấy 1 nút** (`blockMeanAt`): lưới dòng Copernicus 1/12° mịn hơn ô cá 0.25° ⇒ trước đây `nearestIndex` VỨT 8/9 nút; đo được tự tương quan trường hội tụ gốc 0.680 → sau lấy mẫu điểm 0.228 (nhiễu trắng ở đúng độ phân giải bà con nhìn) → gộp khối 0.467. Dùng TRUNG BÌNH (giữ kỳ vọng ⇒ không lệch phân bố đã hiệu chỉnh); max/p75 có lý sinh học nhưng khuếch đại nhiễu + phải hiệu chỉnh lại — CHƯA đo, để dành. Đo API thật: %điểm nóng 21.8 → 21.5, 23 loài giữ nguyên, dataQuality 1.
(3) **3 hằng gradient còn lại khai theo ĐỘ** (`THERM_FRONT_FULL_PER_DEG` 2.0 · `CHL_FRONT_FULL_PER_DEG` 1.0 · `EDDY_FULL_PER_DEG` 0.16, × `gridStepDeg`): hằng ghi theo "mỗi Ô" LỆ THUỘC BƯỚC LƯỚI ⇒ đổi độ phân giải nguồn là thang tự đổi ÂM THẦM. Ba số chọn để GIỮ NGUYÊN hành vi hôm nay (2.0×0.25=0.5 · 1.0×0.25=0.25 · 0.16×0.5=0.08). Ghi chú: eddy trước tương đương 0.16 m/độ và tình cờ đúng 1.05× p90 — ĐÚNG DO MAY, không do thiết kế. -->

<!-- re-verified: 2026-07-26d — CỔNG MỒI THÔI BÃO HOÀ (`CHL_RANK_PLATEAU_FRAC` = 0.5).
VẤN ĐỀ (cổng kiểm độ phân biệt chỉ đích danh): cao nguyên `chlFit` trải TRỌN dải `chlLog` khai báo, mà dải đó RỘNG HƠN CẢ PHÂN BỐ thật của biển VN (log10(chl) p05 −1.01 · p50 −0.67 · p90 −0.08; p25→p75 chỉ 0.43 đơn vị log) ⇒ 83–99% ô đạt `chlFit = 1`, cổng mồi KHÔNG phân biệt được ô nào giàu hơn (`food:mực xà` std 0.019 — thấp hơn cả `thermoFit` trước khi sửa). Lần thứ BA cùng khuôn lỗi.
SỬA: cao nguyên co về đầu GIÀU MỒI, phần còn lại của dải thành đoạn DỐC. MỘT knob duy nhất áp đều 40 loài (kiểm lại được, không phải 13 số thủ công). TRUNG THỰC: dải `chlLog` GIỮ NGUYÊN vai trò ngưỡng chịu đựng sinh học; thay đổi duy nhất là bên TRONG vùng chịu đựng nay có độ dốc — "trong mức chịu được thì nhiều mồi hơn là tốt hơn". KHÔNG phải phát biểu sinh học mới.
CHỐT 0.5 BẰNG SỐ (sweep trên API thật): 0.35 → hot 15.3% nhưng **MẤT 1 loài** (23→22) ⇒ loại; **0.5 → hot 17.9%, GIỮ đủ 23 loài**, median 34, payload 335 KB; 0.65 → hot 19.5% (phân biệt kém hơn). Cổng kiểm sau khi sửa: cờ đỏ 19 → 13; `food:mực xà` std 0.019→0.076 và bão hoà 99%→**5%**; 7 cờ mồi sạch hẳn (cá hồng · cá mối · ngừ vây vàng · nục heo · cá thu · ngừ ồ · ngừ chù · ngừ chấm).
CÒN MỞ (ghi để không ai chạy vòng vô ích): 13 cờ đỏ còn lại KHÔNG phải bão hoà nữa mà là std hơi dưới 0.10 — đó là **giới hạn dải động của trường chl ở biển VN** (chỉ trải 0.43 đơn vị log), không phải lỗi dải khai. Riêng 6 cờ `tFit:*` (ngừ chù 0.027 · cá cơm 0.040 · cá cờ · cá lầm · mực xà · nục heo) là VẤN ĐỀ RIÊNG chưa xử: dải nhiệt loài rộng hơn phân bố nhiệt mặt Biển Đông 27–30°C — cùng khuôn lỗi, cần một mạch riêng có calibrate.
Test 601 pass; 7 test tổng hợp phải cấp lưới mồi ĐỦ GIÀU để cô lập đúng biến đang đo (chl 0.1/0.8 nay nằm ở đầu NGHÈO) — KHÔNG nới lỏng assertion nào. -->

<!-- re-verified: 2026-07-28 — KIỂM CHỨNG THÔNG SỐ CÁ NGỪ (trước khi build tính năng lộ trình câu cá ngừ).
Đối chiếu SPECIES_PROFILES + FISH_SEASONS với RIMF, WCPFC, ICCAT, FishBase, SPC. CHƯA SỬA GÌ — đây là biên bản kiểm.

🔴 MÙA VỤ HỎNG Ở 3/6 LOÀI NGỪ — nặng nhất, và đúng loài quan trọng nhất:
 · vây vàng + mắt to khai ĐỦ 12 THÁNG ⇒ seasonPrior luôn = 1 ⇒ TERM MÙA VỤ CHẾT (chỉ 2/44 dòng mùa vụ
   toàn mô hình bị vậy, và đúng 2 loài này). Ghi chú trong chính dữ liệu lại nói "rộ tháng 12–6" ⇒ dữ liệu
   TỰ MÂU THUẪN. Thực tế: Bình Định "từ tháng 7 DL biển rất vắng cá ngừ đại dương"; Đào Mạnh Sơn 2004 vụ
   chính 11–4; WCPFC 2012 đỉnh 12–2. Đề xuất vây vàng 11–6 cao / 7–8 giảm / 9–10 thấp; mắt to 11–7
   (mắt to là ngoại lệ: WCPFC dẫn Toàn 2011 nói hè 4–7 CAO HƠN đông).
 · cá ngừ VẰN khai 11–5 là NGƯỢC MÙA — bỏ trắng nguyên đỉnh 7–9. Nguồn: Bình Định "từ tháng 7 là mùa cá
   nổi (chủ yếu ngừ sọc dưa)"; Vinatuna rộ đến hết tháng 9; WCPFC 2012 hai đỉnh 12–1 và 7–8.
 · cá ngừ Ồ khai 11–5 cũng NGƯỢC — nguồn VN nói rộ tháng 3–8.

🟡 CỔNG NHIỆT LỆCH ẤM (đo độc lập trên SST thật xác nhận): tháng 7 biển VN chỉ trải 28,7–30,0 °C mà
 plateau vây vàng là 26–30 ⇒ 90,4 % ô đạt điểm tối đa, độ lệch chuẩn 0,081 ⇒ TERM NHIỆT KHÔNG PHÂN BIỆT
 (cá ngừ chù còn tệ hơn: 97,8 %). Đúng mẫu lỗi "always-on term" đã ghi trong memory. ICCAT (systematic
 review) cho SST ưa thích vây vàng 24,4–28,6 · vằn 23,0–28,3 · mắt to 24,2–28,5.
 Đề xuất: vây vàng [22, 24.5, 29, 31] · vằn [20, 23, 28.5, 31] · chấm [18, 24, 29, 31].
 ⚠ PHẢI PHÂN BIỆT ambient vs SST: "mắt to ưa 17–22 °C" là nhiệt Ở TẦNG SÂU, KHÔNG phải SST — đừng ai sửa
 cổng SST theo con số đó.

🟡 THÔNG SỐ NGHỀ (dùng cho spec lộ trình 09 §4):
 · "sashimi ≤7 ngày sau con cá đầu" CHẶT GẤP ĐÔI chuẩn — SPC "Onboard handling of sashimi-grade tuna":
   xử lý đúng (cắt tiết, moi mang/ruột, hạ tâm về 0 °C bằng slurry) giữ chuẩn sashimi TỚI 2 TUẦN.
   Đề xuất ≤14 ngày, hoặc ≤10 nếu tàu chỉ có đá xay. Giữ 7 ngày = khuyên bà con cắt đôi chuyến vô căn cứ.
 · "chạy ra ~3 ngày" QUÁ DÀI — kỹ thuật nghề câu VN: Quy Nhơn → ngư trường 30–48 giờ ⇒ ~2 ngày.
 · CHƯA TÁCH 2 NGHỀ: câu vàng (longline, vàng 40–55 km, thả 14–17h30, chuyến >20 ngày, theo vụ, giá cao)
   vs CÂU TAY KẾT HỢP ÁNH SÁNG (từ 2011, Hoài Nhơn; 4 cần + mồi mực sống + giàn 15–20 bóng cao áp; bắt
   buộc ĐÊM; ~20 ngày; QUANH NĂM; thịt bị chua, giá chỉ 40–60 % câu vàng). Hai nghề khác nhau về mùa,
   chuyến, cách đánh ⇒ preset lộ trình phải tách.
 · Cổng độ sâu vây vàng [50,200] m: ngư trường THẬT 200–4.000 m (Hoàng Sa 400–4.000, Trường Sa 200–3.000)
   ⇒ cổng trả 1,0 trên 100 % ngư trường thật, chỉ còn tác dụng chặn thềm. Đề xuất [100,400] / mắt to
   [200,600] — NHƯNG cân nhắc: nâng lên sẽ chặn oan tàu câu tay/lưới vây nước nông (quyết định sản phẩm).
 · chlLog vây vàng [-1.1,-0.1] = 0,079–0,79 mg/m³ nhưng khơi Biển Đông chỉ 0,11 ± 0,06 ⇒ NỬA TRÊN không
   bao giờ dùng tới. Đề xuất [-1.3,-0.5].
 · thermoBand D20 [4,12] m: HƯỚNG đúng (y văn: nêm nhiệt sâu ⇒ CPUE mắt to cao hơn) nhưng cửa sổ 4–12 m
   KHÔNG CÓ NGUỒN và hẹp so dị thường D20 do xoáy trung quy mô. Nới hoặc đổi sang phân vị.

✅ ĐÚNG, KHÔNG ĐỤNG: SST mắt to [22,25,29,31] (khớp ICCAT 24,2–28,5 — thông số tốt nhất app đang có) ·
 mùa cá ngừ chù (3–9) và chấm (1–5,10–12) · mô tả depthBand cả hai loài · chlLog mắt to · "đánh đêm" ·
 ">200 hải lý" · "chuyến 15–30 ngày" · hướng dương của thermoBand.

⚠ TRƯỚC KHI SỬA BẤT KỲ HẰNG SỐ CHẤM ĐIỂM NÀO: chạy scripts/model-discrimination-audit.mjs (guard
 always-on-term) — luật của dự án sau khi dính bẫy này 4 lần trong một ngày. Nguồn đầy đủ: transcript
 phiên 2026-07-28. -->
