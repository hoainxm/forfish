# Ops — External Services — ForFish

> Load khi: lỗi liên quan nguồn dữ liệu ngoài (timeout, rate limit, đổi format, token hết hạn), thêm nguồn mới, hoặc audit phụ thuộc.

covers: src/lib/abort.ts, src/lib/tile-proxy.ts, src/lib/offline-basemap.ts, src/lib/sea.ts, src/lib/marine-weather.ts, src/lib/route-weather.ts, src/lib/forecast-grid.ts, src/lib/scalar-field.ts, src/lib/copernicus-salinity.ts, src/lib/copernicus-wav.ts, src/lib/copernicus-cur-depth.ts, src/lib/cur-depth.ts, src/lib/snapshot-merge.ts, src/lib/pretrip.ts, src/lib/forecast-ensemble.ts, src/lib/forecast-quality.ts, src/lib/sdwork-assets.ts, src/lib/auth-gateway.ts, src/lib/fish-predict.ts, src/lib/fish-forecast-run.ts, src/lib/fish-snapshot.ts, src/lib/fish-snapshot-policy.ts, src/lib/weather-snapshot.ts, src/lib/weather-snapshot-id.ts, src/lib/hycom.ts, src/lib/copernicus.ts, src/lib/source-registry.ts, src/lib/sst-tendency.ts, src/lib/sea-scalars.ts, src/lib/fuel-price.ts, src/lib/port-price-source.ts
last_verified: 2026-08-18
ttl_days: 180
gate: warn
<!-- re-verified: 2026-06-17 - auth-gateway.ts = SĐT+mật khẩu (signup/sso), bỏ OTP/magic-link/service-key; timeout 20s -->
<!-- re-verified: 2026-08-10b — CHỮ HƯỚNG GIÓ khớp vệt bản đồ (VĐ báo lần 2): `windDescribeVN` đổi "gió Tây Nam" → "gió Tây Nam (thổi về Đông Bắc)". CHỈ ĐỔI CHUỖI HIỂN THỊ — KHÔNG nguồn/endpoint/timeout/request/payload nào đổi; nguồn gió vẫn `wind_direction_10m*` (tới từ), bản đồ vẫn +180° (thổi tới). -->
<!-- re-verified: 2026-08-10 — ĐIỂM CHẠM HIỆN HƯỚNG GIÓ CẢ NGÀY (VĐ báo: hôm nay có hướng, ngày sau trống). Request GIÓ của fetchSeaPointLive (marine-weather.ts) thêm biến daily `wind_direction_10m_dominant` vào chuỗi `&daily=` ĐÃ CÓ — KHÔNG request mới, KHÔNG endpoint/timeout mới, cùng 1 lời gọi Open-Meteo forecast. SeaPointDay thêm `windDirDeg?` optional (cùng khuôn `curDirDeg`) → bản lưu / bản dựng-từ-lưới đời cũ đọc vẫn chạy, thiếu thì UI ẩn mềm. Tải trọng lên Open-Meteo KHÔNG đổi (thêm 1 biến vào request sẵn có). -->

<!-- re-verified: 2026-08-03p — **ĐƯỜNG BUILD KHÔNG ĐI QUA NGUỒN NGOÀI** (bất biến mới). `/api/fish-forecast` được Next dựng sẵn lúc `next build` (có `revalidate`); khi đọc snapshot Supabase hỏng thì nó rơi vào `computeFishForecast()` = BẢY nguồn (ERDDAP + HYCOM OPeNDAP + Copernicus Zarr) trong ngân sách 60 giây của Next ⇒ bản build ĐỎ, deploy không đi được. Nay `isBuildPhase()` chặn nhánh live lúc build: có snapshot thì gieo bằng snapshot, không thì 503 `s-maxage=60` để request thật đầu tiên tính. Đo thật lượt lạnh của nhánh live: 16,8 giây (và đó là khi mọi nguồn còn khoẻ). Không đổi hành vi lúc chạy: `NEXT_PHASE` chỉ mang giá trị đó trong lượt build. -->

<!-- re-verified: 2026-08-03m — NHỊP NGUỒN THEO NGÀY + LƯỚI DÒNG CHẢY TẦNG DÀY GẤP ĐÔI.
  (1) **NHỊP NGÀY tách khỏi lịch GFS** — `source-cadence.isDailyCacheCurrent` (mốc phát hành 12:00 UTC, trần 26 giờ) nay là luật độ tươi cho hai nguồn Copernicus theo NGÀY: **dòng chảy tầng sâu** (`cur-depth.ts`, cả bản trong máy lẫn snapshot) và **độ mặn** (`scalar-field.fetchSalinityField`). Trước đây cả hai đo bằng `isCacheCurrent` — lịch GFS 4 mốc/ngày (04/10/16/22 UTC) — trong khi nguồn `P1D` một ngày ra ĐÚNG một bản. Hệ quả đo được trên kho thật 2026-08-03: `curdepth:t50:d10` cron ghi 08:51 UTC, tới 10:00 UTC bị tuyên "hết hiện hành" ⇒ máy vứt bản 10 KB đang có, đi 10 s snapshot + 45 s route live (server đọc 20 chunk × 1,37 MB TUẦN TỰ) để nhận lại đúng con số cũ, ngày bốn lượt, mỗi lần đổi chip tầng một lượt. Không đổi cron, không đổi khoá snapshot.
  (2) **`fetchCurDepthGrid` lấy mẫu lưới RIÊNG 25×23 = 575 điểm** (`curDepthPoints`, bước ~1,04°, đúng khung `GRID_BOUNDS_DEG`) thay cho lưới chung 13×12. **Không tốn thêm một byte nào từ Copernicus**: server vốn tải NGUYÊN chunk toàn cầu cho mỗi (ngày × tầng) rồi mới sample. Payload trả về còn NHẸ ĐI nhờ bỏ 4 khoá gió/sóng luôn rỗng (`GridHour` nay tuỳ chọn): đo thật qua `/api/currents-depth?tier=50&days=3` — 575 ô, 51% ô không có số (đáy nông hơn tầng 47,4 m: vịnh Bắc Bộ, vịnh Thái Lan, thềm Sunda, thềm Phan Thiết — ĐÚNG vật lý), payload thô **48,8 KB** so với ~46 KB của bản 156 ô verbose. Kho trong máy giữ JSON THÔ nên chỗ tiết kiệm này là tiết kiệm thật, không phải nhờ nén đường truyền.
  (3) **Độ mặn bổ sung nấc đọc-kho-khi-mất-sóng** (`forecastStoreReady()` + `isDefinitelyOffline()`) — trước đây `fetchScalarField` rẽ sang nhánh độ mặn ngay dòng đầu nên lớp này chưa từng có nấc K3 mà các lớp khác đã có từ 2026-08-02: giữa biển bật lớp là 10 s + 35 s cùng hỏng rồi mới chạm bản trong máy, mà kho IndexedDB chưa nạp xong thì trượt luôn cả bản đó. -->

<!-- re-verified: 2026-08-02g — ĐỒNG HỒ CHẶN ĐỔI CÁCH LẤY, KHÔNG ĐỔI GIÁ TRỊ: 65 lời gọi `AbortSignal.timeout(` trần trong src/ (44 file, client + server) chuyển hết sang `timeoutSignal(ms)` của `@/lib/abort`. KHÔNG nguồn mới, KHÔNG endpoint mới, KHÔNG một mốc ms nào đổi, KHÔNG thêm/bớt `.catch`. Lý do: hàm tĩnh đó chỉ có từ Safari 16 / Chrome 103, iPhone iOS 15.8 của bà con ném TypeError — ở `depth-grid.ts` cú ném là ĐỒNG BỘ (hàm chưa `async`) nên vượt qua `.catch` của chỗ gọi và làm SẬP cây React ⇒ bản đồ trắng cả chuyến; `fetchDepthGrid` nay là `async`. Đổi cả nhánh server (Node ≥18 vốn có API) để dựng được cổng TUYỆT ĐỐI thay vì cổng phải nuôi danh sách miễn trừ. Cổng: `src/lib/__tests__/no-bare-abort-timeout.test.ts` (có ca đối chứng). Xoá bản `timeoutSignal` private trong `lib/sea.ts` — bản đó trả `undefined` khi thiếu API tức là MẤT trần thời gian, bản dùng chung có đường lùi AbortController. -->

> Registry CANONICAL cho mọi service ngoài hệ (nguyên tắc 11). Toàn bộ fetch nguồn ngoài BẮT BUỘC có ĐỒNG HỒ CHẶN + degrade rõ ràng (xem 02-architecture §5). **Đồng hồ đó viết bằng `timeoutSignal(ms)` từ `@/lib/abort`, KHÔNG gọi `AbortSignal.timeout(` trần** — xem §"Đồng hồ chặn" bên dưới.

## Đồng hồ chặn: `timeoutSignal(ms)`, không `AbortSignal.timeout(` trần (2026-08-02)

**Luật**: mọi lời gọi mạng trong `src/` — client lẫn server — lấy tín hiệu hết giờ từ `import { timeoutSignal } from "@/lib/abort"`. `src/lib/abort.ts` là file DUY NHẤT được nhắc tên `AbortSignal.timeout`.

**Vì sao**: `AbortSignal.timeout` là hàm tĩnh chỉ có từ **Safari 16 / Chrome 103**. Máy của bà con còn nhiều iPhone kẹt iOS 15.8 (Safari 15) và WebView Android đời cũ — ở đó gọi thẳng là ném `TypeError`. Hậu quả không phải "chậm" mà là:

- `lib/depth-grid.ts` — `fetchDepthGrid` từng **không phải hàm `async`** nhưng trả `Promise`: cú ném bay ĐỒNG BỘ ra khỏi thân hàm trước khi promise kịp tồn tại ⇒ `.catch` của chỗ gọi không với tới ⇒ **cây React sập, bản đồ trắng cả chuyến**. Nay hàm là `async` (lá chắn thứ hai: `timeoutSignal` không ném, `async` bọc mọi cú ném còn lại).
- Chỗ khác thì `TypeError` **đội lốt "mất sóng"**: máy đó không bao giờ tải nổi bản mới mà không ai hiểu vì sao (đã thấy ở `use-tier`: vòng thử lại 10 phút vĩnh viễn dù đang ngồi ngay cảng đầy sóng).

**`timeoutSignal` không bao giờ ném** và có **ĐƯỜNG LÙI THẬT** (`AbortController` + `setTimeout`, có `unref`) — máy cũ vẫn CÒN trần thời gian. Chỉ trả `undefined` khi môi trường thiếu cả `AbortController`. Bản riêng cũ trong `lib/sea.ts` chỉ trả `undefined` (= mất trần) nên đã xoá.

**Cách viết**:

```ts
fetch(url, { signal: timeoutSignal(15000) })            // signal: undefined là hợp lệ

const s = timeoutSignal(12000);                          // Supabase: .abortSignal()
let q = supabase.from(T).select(...).limit(100);         // KHÔNG nhận undefined sạch
if (s) q = q.abortSignal(s);
const { data, error } = await q;
```

**Cổng chặn tái phát**: `src/lib/__tests__/no-bare-abort-timeout.test.ts` quét mọi `src/**/*.ts|tsx` (bỏ chú thích + ruột chuỗi), cấm `AbortSignal.timeout(` trần ngoài `lib/abort.ts`, và cấm khai lại `timeoutSignal` cục bộ. Cổng có **ca đối chứng** (chuỗi mẫu có lỗi phải bị bắt; nhắc tên trong chú thích/chuỗi thì không; số file quét phải > 100) để nó không chết câm khi bộ dò hỏng. **Không đổi giá trị ms nào, không đổi luồng lỗi** — chỉ đổi cách lấy tín hiệu.

<!-- re-verified: 2026-06-16 — covers MỞ RỘNG thêm fish-predict/hycom/sea-scalars/fuel-price/port-price-source (trước bỏ sót → drift im). Toàn bộ fetch ngoài đã có AbortSignal.timeout (sweep 2026-06-16: server 15-20s, client 15-25s). -->
<!-- re-verified: 2026-07-25 — forecast-grid.ts (lớp Windy) nay CHỌN KHUNG 3/5/7/10/16 ngày, dùng model sóng ncep_gfswave025 (như sea/marine-weather) + bước giờ tăng dần, client timeout 20s. sea/marine-weather mở 16 ngày (WAVE_MODEL). -->
<!-- re-verified: 2026-07-25b — fish-predict.ts tách cá ngừ đại dương → vây vàng + mắt to (39→40 loài); KHÔNG đổi tích hợp nguồn ERDDAP/HYCOM (URL/UA/timeout giữ nguyên), chỉ là tham số khẩu vị loài. -->
<!-- re-verified: 2026-07-25c — THÊM nguồn runtime ETOPO 2022 (PIFSC ERDDAP) cho fish-forecast: cổng độ sâu chặn loài xa bờ (cá ngừ/cờ/nục heo/mực xà) khỏi ô cạn sát bờ. Row ETOPO thêm vào bảng; degrade .catch→null. -->
<!-- re-verified: 2026-07-25j — CHUẨN BỊ ĐI BIỂN (lib/pretrip.ts): KHÔNG nguồn/endpoint/timeout mới — chỉ gọi lại đúng fetchSeaPoint / fetchFishForecast / fetchForecastGrid, TUẦN TỰ (không song song, khỏi dội Open-Meteo): mỗi chỗ ghim 2 request Open-Meteo + 1 lần /api/fish-forecast + 3 khung lưới (3/7/16) × 2 request = ~2–3 MB/lượt, bà con chủ động bấm ở bờ. Fallback offline forecast-grid SIẾT: chỉ nhận bản lưu ĐÚNG khung ngày đã xin (bỏ loadLatest) — thiếu thì báo thật + liệt kê khung đang có. /api/fish-forecast trả thêm generatedAt để UI nói tuổi thật của bản do SW giữ lại (⚠️ CẬP NHẬT 2026-07-25/2026-07-31: chủ dự án chốt KHÔNG hiện tuổi ảnh ra màn hình; `generatedAt` nay dùng cho hai việc KHÁC — `savedFishMark().dataAt` để bảng "trong máy có gì" không báo tươi giả, còn tầm ngày pha trộn thì đếm từ `date` = ngày ẢNH qua `fishLeadDays`). -->
<!-- re-verified: 2026-07-25d — VIỆC 2: fish-predict.ts đổi CÁCH XỬ LÝ anomaly (noaacrwsstanomalyDaily) + SSHA (noaacwBLENDEDsshDaily) — nay lấy dị thường KHÔNG GIAN (so vùng bên cạnh) thay vì so cả-bồn. KHÔNG thêm nguồn/URL/UA/timeout mới, không thêm fetch; chỉ hậu xử lý grid tại chỗ (spatialAnomaly, ~0.2s cả anom+sla, trong ngân sách route 60s). -->
<!-- re-verified: 2026-07-25k — NỀN BẢN ĐỒ LÚC MẤT SÓNG: (a) glyph font BỎ CDN fonts.openmaptiles.org (dò 2026-07-25: trả HTML redirect, không phải .pbf → nhãn số mét chưa từng hiện) → tự host public/fonts; (b) tile hải đồ EMODnet + phao OpenSeaMap ĐỔI ĐƯỜNG ĐI: client → /api/tiles/{chart|seamark}/{z}/{x}/{y} (same-origin, SW giữ được, danh sách TRẮNG trong lib/tile-proxy.ts); (c) nền CARTO giữ cross-origin cố ý (ToS) → bù bằng nền tối giản trong máy vn-coast.v1.json; (d) proxy contour cũ vẫn CHƯA nối vào style, có lý do. -->
<!-- re-verified: 2026-07-26b — CÁCH DEGRADE ĐỔI (không thêm/bớt nguồn, không đổi URL/UA/timeout): thiếu nguồn nay phải làm ĐIỂM GIẢM chứ không tăng. wMax của soft-OR cố định theo hồ sơ loài (trước suy từ term còn lại → mất nguồn = điểm tăng +43%); thiếu lưới độ sâu ETOPO → DEPTH_UNKNOWN_FIT 0.5 thay vì bỏ cổng. Cột "Khi hỏng" của HYCOM + ETOPO đã sửa cho khớp. -->
<!-- re-verified: 2026-07-28 — THÊM nguồn ERDDAP LỊCH SỬ (chỉ dùng OFFLINE, không đụng runtime): CoralTemp `noaacrwsstDaily` (1985→nay) + chl THÁNG `noaacwNPPVIIRSSQchlaMonthly` (2012→nay) dựng bản đồ MÙA VỤ 12 tháng (public/data/fish-climatology.v1.json, 69×65 ô 0,25°, ~71 KB, SW pre-cache sdfish-v6) qua scripts/collect-fish-climatology.mjs; scripts/fit-fish-blend-weights.mjs backtest 12 mốc gốc × 7 tầm ngày ra tỷ lệ pha trộn w(d). ĐO THẬT (16 mốc gốc, 30–33k ô/tầm): w giảm 0,823 (d1) → 0,547 (d16) tự nhiên đơn điệu, blend THẮNG persistence ở MỌI tầm khi kiểm chéo (+4,4% → +9,0% RMSE), guard always-on-term PASS (biên độ w 0,276). KẾT LUẬN ÂM ghi lại: tách w theo MÙA GIÓ thua bảng chung ở cả 7 tầm khi kiểm chéo (−2,4% … −6,5%) ⇒ giữ MỘT bảng. KHÔNG route mới, KHÔNG đổi payload /api/fish-forecast, runtime KHÔNG gọi thêm nguồn nào. -->
<!-- re-verified: 2026-07-26c — TIMEOUT NGUỒN HAY TREO: HYCOM (OPeNDAP tds.hycom.org) + Copernicus (ARCO Zarr) — cả hai TUỲ CHỌN — nay dùng SLOW_SOURCE_TIMEOUT_MS=12s thay vì GRID_TIMEOUT_MS=20s. Lý do: route /api/fish-forecast gom mọi trường bằng Promise.all → resolveField chờ MỌI load() settle → một nguồn treo 20s KÉO cả route quá mốc hủy 35s của client (fetchFishForecast) ⇒ "dự báo cá chưa tải được" dù SST/phù du (bắt buộc, ~3s) đã sẵn. Chẩn 2026-07-26: HYCOM .dds treo >25s. ERDDAP bắt buộc giữ 20s (nhanh + cốt lõi). fetchHycomGrids(timeoutMs?) + fetchCopernicusCurrents({timeoutMs}). -->
<!-- re-verified: 2026-07-25n — TẢI SẴN nay TỰ CHẠY (bỏ nút "Chuẩn bị đi biển"): KHÔNG nguồn/endpoint/timeout mới, vẫn đúng fetchSeaPoint / fetchFishForecast / fetchForecastGrid chạy TUẦN TỰ, ~2,5–3 MB/lượt. ĐIỂM CẦN NHỚ về tải trọng nguồn: trước bà con chủ động bấm, nay máy tự gọi khi VÀO màn Ra khơi → cửa chặn BẮT BUỘC ở lib/pretrip-auto.ts (shouldAutoPretrip): chỉ chạy khi chưa có bản nào hoặc bản cũ hơn PRETRIP_MIN_INTERVAL_MS=6h (khớp ISR 6h /api/fish-forecast), navigator.onLine=false thì không thử, và chỉ 1 lần mỗi lần mở app → trần thực tế ~4 lượt/máy/ngày thay vì mỗi lần mở màn. Sửa cửa chặn này = đổi tải trọng lên Open-Meteo/ERDDAP, phải cân nhắc kèm. -->
<!-- re-verified: 2026-07-26b — THÊM mục "Copernicus `thetao` — chỉ dùng ĐỂ ĐO, KHÔNG vào runtime": kho ARCO ngày cmems_mod_glo_phy-thetao_anfc_0.083deg_P1D-m (downsampled4) dùng cho scripts/fish-3day-probe.mjs, chi phí đo thật 4,58 MB / 7,0–7,3 s cho 4 mốc ngày. KHÔNG thêm fetch nào vào /api/fish-forecast (route vẫn 11 fetch NOAA/HYCOM/ETOPO, cold 3,4 s đo thật) vì bản đồ cá D+3 gần như không đổi. lib mới src/lib/sst-tendency.ts vào `covers`; fail-safe: thiếu Copernicus → persistence, không vỡ. -->
<!-- re-verified: 2026-07-26c — route-weather.ts (lưới thời tiết DẪN ĐƯỜNG) thêm CACHE promise TTL 45 phút, khoá bbox+ngày-VN: "Tính lại" / vòng nở khung của route-planner không refetch ~0,7–1,5 MB nữa; lỗi mạng không găm cache (pattern fetchDepthGrid). KHÔNG đổi endpoint/UA/timeout. Hardening cùng đợt: ô sóng-có-số nhưng giờ gió RỖNG → onSea=false; giờ thiếu số sóng → route-plan ước từ gió (estimateWaveFromWind) thay vì 0. -->
<!-- re-verified: 2026-07-26 — SỔ NGUỒN + SO NGÀY (lib/source-registry.ts): mỗi TRƯỜNG của /api/fish-forecast nay có DANH SÁCH ứng viên, chạy song song, lấy bản có ngày MỚI NHẤT, quá tuổi thì vẫn dùng nhưng gắn `stale`. THÊM 2 nguồn ERDDAP dự phòng ĐÃ FETCH THỬ THẬT (200, ~255–300 KB, 3–4 s): SST `noaacrwsstDaily` (CoralTemp, **đơn vị °C không kelvin**) + phù du `noaacwNPPN20S3ASCIDINEOFDaily` (thêm cảm biến Sentinel-3 OLCI). Tổng fetch 9 → 11, VẪN song song nên wall-clock không đổi (đo thật 2026-07-26: route trả trong ~5 s ấm cache). Payload thêm `sources` / `dataQuality` / `targetDate`. -->
<!-- re-verified: 2026-07-28b — THÊM lib/scalar-field.ts: LỚP DẢI MÀU (mây/mưa/nhiệt không khí) kiểu Windy. Nguồn = Open-Meteo forecast (CÙNG endpoint api.open-meteo.com đã dùng cho gió), gộp 5 biến hourly cloud_cover,precipitation,temperature_2m,cape,pressure_msl vào MỘT request (cape = CAPE J/kg cho lớp "Dông" = NGUY CƠ dông/sét, KHÔNG phải sét thật — `lightning_potential` đo thật trả NULL ở VN, chỉ có model châu Âu; sét quan sát thật = Blitzortung, ngoài phạm vi; pressure_msl = áp suất mực biển hPa). ĐỘ MẶN ĐÃ NỐI (Copernicus, hàng riêng bên dưới, probe thật scripts/copernicus-salinity-probe.mjs). Cùng stepHourIndices + timeout 20s + cache offline (lib/forecast-cache, NS "scalar"). Render: nội suy song tuyến → fill + lớp WebGL nền mịn. Loại trừ lẫn nhau với lớp gió/sóng (một overlay/lần). ⚠️ TẢI TRỌNG (2026-07-29): lưới MỞ RỘNG 80→156 điểm (98–123°Đ/1–24°B) × 5 biến × tới 16 ngày — Open-Meteo tính theo trọng số nên ĐÃ DÍNH 429 "Daily API request limit" khi dev test dày; hướng xử: cron snapshot server (weather_snapshot, khoá scalar:*) làm fallback + client vẫn live-first. -->
<!-- re-verified: 2026-08-02b — NGƯỠNG "CÒN MỚI" PHÍA CLIENT PHẢI KHỚP NGƯỠNG NGUỒN THẬT SỰ RA BẢN MỚI. `savedLayers()` (lib/pretrip.ts) đo `fresh` của mọi lớp bằng `isCacheCurrent` — nhịp Open-Meteo (04/10/16/22 UTC, sàn 45′, trần 12h). SAI với **bản đồ cá**: nguồn của nó là snapshot do cron tính ~6h/lần, và `/api/fish-forecast` CHỈ bỏ snapshot đi tính live khi quá `SNAPSHOT_MAX_AGE_MS` = **30 giờ** (lib/fish-snapshot-policy.ts). Client khắt khe hơn route ⇒ bảng "trong máy có gì" gọi bản 8–13 giờ tuổi là "đã cũ", chip mời "chạm tải mới", mà route thấy snapshot còn tươi nên trả lại nguyên bản đó — `generatedAt` không đổi ⇒ nút bấm bao nhiêu lần cũng không đổi được gì. VÁ: thêm `isSnapshotFreshAt(ms, now)` (bản SỐ của `isSnapshotFresh`, dùng chung một hằng số) và lớp `fish` dùng nó thay `isCacheCurrent`. KHÔNG thêm nguồn/endpoint/timeout/request nào — chỉ đổi cách ĐỌC tuổi bản đã có trong máy. BẤT BIẾN cho lớp thêm sau này: mỗi lớp phải đo `fresh` bằng nhịp của CHÍNH nguồn nó, và không được chặt hơn ngưỡng route đi lấy bản mới — chặt hơn là dựng ra một nút không bao giờ làm được việc nó hứa. -->
<!-- re-verified: 2026-07-29b — AUDIT OFFLINE các lớp mới: pretrip THÊM bước tải sẵn LỚP DẢI MÀU (fetchScalarField("cloud", d) cho d∈PRETRIP_SCALAR_DAYS=[3,16] — mỗi lượt MỘT request Open-Meteo ra cả 5 lớp, khớp khoá cache kind.d{gridDays} mà màn Ra khơi xin theo hạng free-3/premium-16) + ĐỘ MẶN (fetchScalarField("salinity") — khoá cache CHUẨN HOÁ salinity.d4 duy nhất bất kể màn xin 3/16, hằng SALINITY_DAYS). Trước đó lớp mới KHÔNG được tải sẵn → ra khơi mở lần đầu là trống. Tải trọng pretrip: +2 request Open-Meteo (156 điểm × 5 biến, khung 3+16) + 1 request same-origin /api/salinity (~140 KB); vẫn tuần tự + cửa chặn 6h pretrip-auto. Production build PASS sau thay đổi. -->
<!-- re-verified: 2026-07-29c — LOẠI BẢN LƯU ĐỜI CŨ SAU KHI MỞ LƯỚI (user bắt được production hiện "cửa sổ 80" nhỏ): 429/mất sóng mà nhận bản lưu vùng-phủ-cũ thì lớp màu/hạt co cụm một góc. forecast-grid thêm gridIsCurrent (đủ ô + đúng 4 góc bbox) — fetchForecastGrid fallback + snapshot CHỈ nhận bản khớp lưới hiện tại; tách savedCurrentGridDays (UI lớp vẽ) khỏi savedGridDays (tra ĐIỂM/tuyến VẪN dùng bản cũ — nearestGridCell tự chặn ngoài vùng phủ, không vứt dữ liệu an toàn). scalar-field thêm scalarGridUsable (khớp nLat×nLon) cho cả localStorage + snapshot + salinity. glOk của lớp GL nền mịn nay TỰ LÀNH (suy từ sự hiện diện thật của layer sau mỗi ensure — hết kẹt polygon ô vuông thô). Hệ quả chuyển tiếp MỘT LẦN: máy có cache đời cũ + đang 429 sẽ thấy "Chưa tải được khung N ngày" thay vì cửa sổ nhỏ — trung thực; quota reset / cron snapshot 156 chạy là đầy lại. -->
<!-- re-verified: 2026-07-29d — LƯỚI AN TOÀN NHIỀU TẦNG + TỰ KÉO LẠI (user: "cứ báo thiếu, snapshot có rồi thì lấy snapshot"; "sao nó không tự kéo lại?"). (1) fetchForecastGrid/fetchScalarField nay có ĐỦ 5 tầng: live → localStorage ĐÚNG khung → snapshot ĐÚNG khung → localStorage khung NGẮN HƠN → SNAPSHOT khung ngắn hơn → mới báo lỗi. Chỉ mượn khung NGẮN hơn (mượn khung dài cho tài khoản thường = lộ tầm premium — có test). An toàn từ khi BỎ chip chọn khung: thanh ngày vẽ theo times[] thật nên đưa lưới 3 ngày không còn nói dối như luật cấm cũ 2026-07-25. (2) gridIsCurrent NỚI LẠI: chỉ xét VÙNG PHỦ (4 góc bbox), KHÔNG xét mật độ — bản lưu thưa hơn (110 ô, cùng bbox 98–123/1–24) vẫn dùng tốt vì mọi chỗ dựng hình tự suy kích thước lưới; chỉ bản đời CŨ HẲN (bbox nhỏ 102,5–117,25/6–21,3) mới bị loại vì gây co cụm. (3) Sheet điểm: đang dùng SỐ CŨ thì TỰ THỬ LẠI — quay lại tab · có mạng lại · hoặc mỗi 5 phút, chung một cửa chặn để không dội nguồn đang 429. BUILD OK, 832 test pass. -->
<!-- re-verified: 2026-07-29f — TIẾT CHẾ NGUỒN THEO NHỊP PHÁT HÀNH + TỰ LÀM MỚI KHI CÓ SÓNG (user: "tính toán tối ưu để không request nhiều quá lại bị khoá IP… đã có bản tin lúc x giờ thì sau y giờ mới có bản mới, trong thời gian y không cần kéo"). MỚI lib/source-cadence.ts (thuần, 10 test): Open-Meteo chạy GFS 4 lần/ngày, bản lên API trễ 3,5–5h ⇒ mốc AN TOÀN 04/10/16/22 UTC (OM_RELEASE_HOURS_UTC). isCacheCurrent(savedAt, now) = chưa qua mốc kế tiếp + sàn MIN_REFETCH_MS 45′ + trần MAX_CACHE_MS 12h. fetchForecastGrid/fetchScalarField nay ĐỌC CACHE TRƯỚC: bản còn hiện hành → dùng luôn, KHÔNG gọi Open-Meteo và KHÔNG gắn stale (đúng là bản mới nhất). TRƯỚC ĐÂY hai hàm này không có TTL nào — mỗi lần bật/tắt lớp, đổi khung, hay có sóng lại là một lượt tải cả lưới 156 điểm ⇒ chính là thứ đốt hạn ngạch tới 429. Ước tính giảm: bật/tắt lớp N lần trong một chu kỳ 6h nay còn ĐÚNG 1 request thay vì N. Kèm: pretrip-auto thêm shouldAttemptAutoPretrip (cửa PRETRIP_MIN_RETRY_MS 2′ giữa các lần THỬ, ngoài cửa 6h giữa các lần TẢI XONG) + PretripAutoNotify nghe online/visibilitychange (trước chỉ chạy 1 lần/phiên: mở app lúc mất sóng là cả phiên không kéo lại); fishing-map-view có netEpoch (cửa 2′) đưa vào deps effect lưới gió/lớp màu/bản đồ cá để có sóng lại là làm mới; sheet điểm đang "số cũ" tự thử lại (tab visible / online / 5′). BUILD OK, 848 test pass. -->
<!-- re-verified: 2026-07-29g — RÀ SOÁT NGUỒN + ƯU TIÊN SNAPSHOT + LỚP DÒNG CHẢY (user: "luôn ưu tiên snapshot để hạn chế bị lock do IP tải nhiều; nguồn nào dự báo dài nhất thì lấy; thêm lớp dòng chảy; chạm điểm hiện tốc độ + hướng dòng chảy; offline bám mốc 16 ngày").
  (1) ĐẢO THỨ TỰ FALLBACK client (forecast-grid / scalar-field / sea): cache máy CÒN HIỆN HÀNH → **SNAPSHOT server còn hiện hành** (cron nhét `savedAt` vào payload, so bằng isCacheCurrent) → live Open-Meteo → các nấc stale cũ giữ nguyên. Snapshot đi same-origin (CDN s-maxage 1800 + SW) nên KHÔNG đụng hạn ngạch Open-Meteo theo IP máy; snapshot cũ hơn nhịp phát hành thì vẫn đi live — không hy sinh độ tươi. Sea snapshot đổi shape `{savedAt, days}` (reader nhận cả mảng trần đời cũ).
  (2) CRON THỜI TIẾT thêm GitHub Actions `.github/workflows/refresh-weather.yml` cron `40 4,10,16,22 * * *` — NGAY SAU mốc phát hành GFS (OM_RELEASE_HOURS_UTC 04/10/16/22) để snapshot tươi gần trọn chu kỳ; Vercel Hobby 1 lần/ngày chỉ còn là dự phòng.
  (3) TẦM DỰ BÁO ĐO THẬT 2026-07-29 (nguồn dài nhất từng biến): mưa/mây/nhiệt/CAPE/áp suất Open-Meteo forecast đủ 16 ngày (đã dùng đúng nguồn dài nhất); sóng ncep_gfswave025 16 ngày; DÒNG CHẢY Open-Meteo marine (MeteoFrance SMOC 1/12°, cập nhật 24h/lần) tới ~+9–10 ngày = NGANG Copernicus merged-uv (+9/10) nhưng rẻ hơn hàng chục lần cho lưới nhiều mốc giờ (1 request/156 điểm vs 3,83 MB/MỘT mốc giờ) → lớp bản đồ + điểm chạm dùng Open-Meteo, Copernicus GIỮ NGUYÊN cho term hội tụ /api/fish-forecast. Độ mặn Copernicus vẫn 4 mốc ngày (dataset có xa hơn — chưa mở, chờ nhu cầu).
  (4) LỚP DÒNG CHẢY MỚI (kind `current` trong forecast-grid): fetch marine `ocean_current_velocity,ocean_current_direction` là REQUEST RIÊNG — probe thật: ghim `models=ncep_gfswave025` thì ocean_current_* trả TOÀN NULL. ⚠️ HƯỚNG NGƯỢC QUY ƯỚC: nguồn ghi hướng nước CHẢY VỀ ("0° = Going north") — KHÔNG +180° như gió/sóng (arrowFeatures/buildUVField có nhánh riêng + test). GridHour thêm `curKmh/curDirDeg` optional → bản lưu đời cũ vẫn đọc được; lớp Dòng chảy cần số thật thì `fetchForecastGrid(days, {needCurrent})` bỏ qua nấc cache/snapshot thiếu cur. Ngày 11–16 nguồn không phủ → lớp tự trống (thanh ngày vẫn 16 — offline bám mốc 16 ngày như cũ, pretrip không đổi: cur đi kèm trong CÙNG bản lưới d3/7/16).
  (5) ĐIỂM CHẠM thêm dòng chảy: fetchSeaPoint thêm 1 request marine (current= + hourly=ocean_current_*), mỗi ngày lấy MỘT số đại diện GIỮA TRƯA (dailyCurrentFromHourly — tốc độ + hướng CÙNG mốc giờ); bản dựng từ lưới cũng ra số dòng chảy theo ngày. Sheet: "Dòng chảy lúc này/hôm nay: X km/giờ · nước chảy về hướng Y".
  Tải trọng: +1 request marine mỗi lượt tải lưới + 1 mỗi chạm điểm live, NHƯNG bù lại toàn bộ luồng đọc thường ngày chuyển sang snapshot (0 request Open-Meteo từ IP máy khi snapshot tươi) → tổng tải lên Open-Meteo GIẢM mạnh. -->
<!-- re-verified: 2026-07-29h — GHÉP 2 NGUỒN/LỚP TRONG SNAPSHOT (user: "mỗi lớp lấy 2 nguồn, lưu snapshot để có backup; final = bản MỚI + ngày thiếu lấy từ bản DÀI").
  KIẾN TRÚC: ghép ở CRON, client KHÔNG đổi (vẫn đọc 1 id, 0 request thêm). Luật thuần lib/snapshot-merge.ts (11 test): đơn vị ghép = (NGÀY, nhóm biến); nguồn MỚI NHẤT có phủ thắng; ngày chỉ bản dài cũ hơn có → lấy bản dài; nguồn >48h loại; KHÔNG trộn trong ngày; nguồn lastResort chỉ vào khi không nguồn thường nào phủ; sea CHẤM LẠI điểm sau ghép; savedAt bản ghép = nguồn tươi nhất được dùng (client so nhịp phát hành đúng).
  NGUỒN THEO LỚP: gió/5-lớp-dải-màu = best_match + `ecmwf_ifs025` (probe 2026-07-29: đủ 7 biến kể cả CAPE tới ~+14,5 ngày); sóng = ncep_gfswave025 + `ecmwf_wam025` (~+14,5 ngày; ⚠️ daily=wave_height_max của wam025 trả TOÀN NULL → sea backup gộp MAX từ hourly) + VÉT CUỐI Copernicus WAV (hàng dưới); dòng chảy = SMOC + vét cuối Copernicus merged-uv (một mốc, chỉ NGÀY HÔM NAY); độ mặn/SSHA/bão = 1 nguồn (không có nguồn thứ hai tử tế — CỐ Ý).
  VẬN HÀNH: hàng THÔ mỗi nguồn giữ ở `raw:<id>:<src>` (KHÔNG lọt whitelist /api/weather-snapshot — có test) → nguồn chết một lượt cron vẫn còn đồ ghép; lưới/dải màu fetch backup MỖI lượt (~7 request thêm/run — rẻ), cảng chỉ fetch backup khi nguồn chính chết; WAV/UV lazy-memo — thường ngày 0 byte Copernicus. ĐO THẬT merge 2 nguồn thật (scripts tạm, 2026-07-29): cả hai tươi → OM ăn trọn (ưu tiên khi hoà); OM cũ 20h → ECMWF ngày 1–15 + đuôi 16 và dòng chảy từ OM cũ, sóng phủ kín 16/16 ngày. -->
<!-- re-verified: 2026-07-29i — DÒNG CHẢY THEO TẦNG SÂU + DÒNG CHẢY MẶT ĐỦ ~10 NGÀY KHI SMOC CHẾT + LAN MÀU VEN BỜ (user: "làm dòng chảy theo độ sâu, làm snapshot, đủ 10 ngày, fix màu hổng ô gần đất").
  (1) NGUỒN MỚI Copernicus phy-cur P1D `cmems_mod_glo_phy-cur_anfc_0.083deg_P1D-m` asset downsampled4 (bucket mdl-arco-time-007) — PROBE THẬT: uo/vo `<f4` m/s KHÔNG scale, 50 tầng elevation (mặt Ở CUỐI trục), time NGÀY tới ~+8,8 ngày, 1 chunk toàn cầu/(ngày×tầng) ≈ 1,37 MB/1,7 s; đo cả lượt 1 tầng × 10 ngày = 13,2 s. Lib `copernicus-cur-depth.ts` (fetchCurDepthGrid → shape ForecastGrid times NGÀY, sample 156 điểm lưới; pickElevationIndex/sliceCurDepthDays thuần có test).
  (2) 4 TẦNG danh nghĩa 0/50/150/300 m (thật: 0,5/47,4/155,9/318,1) — CUR_DEPTH_TIERS trong weather-snapshot-id. Cron MỚI /api/cron/refresh-currents-depth (GH refresh-currents-depth.yml `50 5,17 * * *` — nguồn ra bản 1 lần/ngày) ghi `curdepth:t{tier}:d10` (premium, chung luật >3 ngày) + `:d3` (miễn phí, cắt từ d10). Route live /api/currents-depth (premium gate riêng, chunk cache 6h) khi snapshot chưa có. Client lib `cur-depth.ts`: cache→snapshot→live→stale; chip Mặt·50m·150m·300m dưới thanh ngày (chỉ lớp Dòng chảy; tầng mặt vẫn SMOC theo GIỜ, tầng sâu theo NGÀY). Pretrip thêm 1 việc "Dòng chảy theo tầng" (3 tầng sâu, thử d10 rồi d3).
  (3) DÒNG CHẢY MẶT ĐỦ ~10 NGÀY khi SMOC/Open-Meteo chết: refresh-weather thêm vét cuối `copernicus-cur-daily` — CHỈ ĐỌC hàng snapshot curdepth:t0:d10 (không fetch nguồn), gán hằng-số-ngày vào tick; merged-uv theo giờ vẫn ưu tiên cho HÔM NAY (có triều).
  (4) LAN MÀU VEN BỜ (fillCoastalGaps, scalar-field + scalar-gl, có test): ô đất null làm GL discard nửa ô biển kề → nền màu thủng lỗ sát bờ (ảnh user) — nay lan giá trị ô biển kề sang ô null 2 vòng, phần lan trên đất bị lớp bờ vẽ đè. CHỈ áp cho NỀN MÀU; số đọc ra (scalarValueAt/hạt/mũi tên) vẫn lưới gốc. ⚠️ Lưới TẦNG SÂU gắn `noFill` — ô null của nó là "đáy nông hơn tầng" (biển thật, không bị lớp bờ che), lan vào là sai vật lý. -->
<!-- re-verified: 2026-07-29z6 — ĐA NGUỒN + SNAPSHOT các lớp còn thiếu (user "2-3-4 nguồn, luôn có snapshot"). LỚP MÀU (scalar) nay 3 MODEL: best_match + ecmwf_ifs025 (CẮT TRẦN forecast_days=15 — ECMWF IFS ~15 ngày, xin 16 là 400 cả request → GỐC scalar:*:d16 cũ 15h) + NOAA gfs_seamless (nguồn 3, tránh chết nguồn), cron ghép SONG SONG (Promise.allSettled, né 60s). Lớp màu là biến KHÍ QUYỂN → Copernicus Marine không có (đa nguồn = 3 model thời tiết). ĐỘ MẶN + NƯỚC DÂNG/XOÁY (SSHA) nay CÓ snapshot cron (weather_snapshot khoá salinity:d4 / seascalar:ssha; client fetchSalinityField/fetchSeaScalar ưu tiên snapshot trước live) — trước là "1 nguồn không snapshot"; nay live lỗi vẫn có last-good. snapshotNeedsPremium loại salinity/seascalar (công khai). NHÁNH CATCH của scalar/grid/cur-depth nay GHI snapshot khi live 429 (trước trả stale không lưu → "Tải lại" như hỏng). -->
<!-- re-verified: 2026-08-02 — NGUỒN HỎNG PHẢI NÓI RA BẰNG MÃ HTTP, KHÔNG PHẢI 200 KÈM {ok:false} (soát offline MECE, biên bản `ops/audit-offline-2026-08-02.md`). KHÔNG nguồn mới, KHÔNG timeout mới, KHÔNG đổi payload — chỉ đổi MÃ TRẠNG THÁI ở đường lỗi của 4 route, vì service worker chỉ cất phản hồi `res.ok`:
  (1) `/api/fish-forecast` — hết cả snapshot lẫn live: 200 → **503**. Đây là ca CHẶN: payload bản đồ cá CHỈ tồn tại trong kho `sdfish-api-v1` (fish-predict chỉ lưu DẤU `forfish.fc.fishmark.latest`), nên một cú 200-rỗng lúc Supabase + ERDDAP/Copernicus cùng hỏng là ĐÈ MẤT bản duy nhất ⇒ ra khơi lớp cá trắng cả chuyến mà bảng "trong máy có gì" vẫn báo có. Có test HÀNH VI (`api-fish-forecast-status.test.ts`) vì route trả `Response.json(live)` — một BIẾN — nên cổng quét chữ không thấy.
  (2) `/api/sea-scalar` — nguồn hỏng: 200 → **503**; `kind` lạ → **400** (400 KHÔNG nằm trong `isRescuableStatus` nên SW không cứu nhầm bằng bản của kind khác).
  (3) `/api/tiles/{chart|seamark}` — TÁCH HAI Ý NGHĨA: ngoài dải zoom / nguồn trả 404 = ô trống thật → giữ **204**; 429/5xx/hết giờ 12s = KHÔNG HỎI ĐƯỢC → **503**. Vì sao quan trọng: `Response.ok` của 204 là TRUE ⇒ SW cất đè lên ô PNG tốt, mà MapLibre coi thân rỗng là ảnh 1×1 trong suốt và KHÔNG bắn sự kiện lỗi ⇒ ra khơi mất mảng nền hải đồ + phao đèn mà bản đồ không báo gì. Kèm theo: `tileFirst` trong sw.js nay CỨU ô 503 bằng ô đã cất (nếu không thì online-mà-nguồn-hỏng còn tệ hơn trước).
  (4) `/api/weather-snapshot` — `loadWeatherSnapshot` nay phân biệt "hỏi được, chưa có bản" (giữ **404**) với "KHÔNG hỏi được" (thiếu env / Supabase 5xx / fetch ném → **503**). Trước gộp cả năm nguyên nhân thành 404, mà 404 cố ý không nằm trong `isRescuableStatus` ⇒ đây là route duy nhất trong allowlist tự chặn đường cứu của chính mình: máy đang giữ lưới d16 trong kho vẫn nhận 404 khi hạ tầng chập chờn.
  CỔNG CHỐNG TÁI PHÁT: `src/lib/__tests__/api-error-status.test.ts` đọc thẳng mọi route nằm dưới `API_CACHE_ALLOW` và bắt lại khuôn `Response.json({ok:false})` không kèm status — đã dính 3 lần ở 3 route khác nhau, lần này chặn cả khuôn.
  ĐÃ ĐO trên build thật (2026-08-02): `/api/fish-forecast` là route DUY NHẤT trong nhóm dùng `export const revalidate` CẤP ROUTE (các route khác dùng `next:{revalidate}` cấp fetch) ⇒ Next **prerender nó lúc build** (`○ Static`, 30m). Đọc `.next/server/app/api/fish-forecast.meta` thấy Next lưu cả `status` ⇒ **một phản hồi 503 CÓ THỂ bị nướng sẵn và phục vụ tới 30 phút** nếu lúc build cả Supabase lẫn nguồn live đều chết. Build lần này nướng ra `{"status":200}` + payload `ok:true` thật, nên đường thường không dính. Và kể cả ca xấu đó thì 503 vẫn TỐT HƠN HẲN hiện trạng cũ: 503 không được service worker cất nên máy bà con GIỮ NGUYÊN bản tốt, còn 200-kèm-{ok:false} thì đè mất bản duy nhất. Nếu sau này thấy 503 dính 30 phút gây phiền, hướng sửa đúng là cho client đọc thẳng `caches.match("/api/fish-forecast")` khi `!r.ok` — KHÔNG phải bỏ ISR (ISR là thứ giữ cho chốt premium ở middleware chạy trước cache). -->

<!-- re-verified: 2026-08-02f — HÀNH VI CACHE/TIMEOUT CỦA SERVICE WORKER: BA VÁ + MỘT QUYẾT ĐỊNH CÓ Ý THỨC. KHÔNG nguồn mới, KHÔNG timeout nguồn ngoài nào đổi, KHÔNG đổi payload. Chi tiết kiến trúc ở 02-architecture §2026-08-02f.

  (1) **GIỮ-SỐNG ĐĂNG KÝ SAI CHỖ (lỗi CHẶN)**. Bốn nhánh mạng của `public/sw.js` — ô bản đồ (`tileFirst`) · điều hướng (`navigationFirst`) · `/api/*` · asset — đều đăng ký `keepAlive` BÊN TRONG `.then` của `net`. Từ khi mọi nhánh đua đồng hồ (`raceTimeout`), đồng hồ thắng là promise `respondWith` settle NGAY, nên lúc mạng về thật thì `event.waitUntil` ném `InvalidStateError` và `keepAlive` nuốt ⇒ **cú ghi kho KHÔNG được bảo vệ**: trình duyệt được phép giết SW giữa chừng và bản mới không vào kho. Ngoài khơi mạng gần như LUÔN về sau đồng hồ điều hướng (`NAV_NETWORK_MS` 2500 ms) ⇒ kho vỏ đứng yên ở bản của lần cài. Nay `keepAliveLate(event, net, limitMs)` đăng ký ngay khi tạo `net`, và bọc thêm `raceTimeout(net, limitMs*2)` — bắt buộc, vì `raceTimeout` CỐ Ý không hủy `fetch` nên ca "sóng sống mà chết" sẽ ghim SW sống vô hạn (đốt pin giữa biển). Trần theo nhánh: `TILE_NETWORK_MS` · `NAV_GIVEUP_MS` (không phải `NAV_NETWORK_MS`: nhánh này còn cuộc đua thứ hai tới 8 s) · `API_STALE_MS` · `ASSET_NETWORK_MS`/`RSC_NETWORK_MS`.

  (2) **MÁY HẾT CHỖ LÚC CẤT**. Ba nhánh cất `put` rồi mới `trim`, cả cụm trong `keepAlive` = `.catch(()=>{})` ⇒ QuotaExceeded nuốt sạch, không ai biết bản mới không nằm xuống. **Đảo thứ tự KHÔNG đủ** (ghi rõ để người sau đừng "đơn giản hoá"): `trimCache` đếm SỐ MỤC (kho API trần 120) trong khi payload lưới gió/sóng là vài MB/bản ⇒ hạn ngạch trình duyệt cạn từ rất lâu trước khi chạm 120 ⇒ trim giải phóng 0 byte. Nay `putWithRoom` bắt lỗi thật → bỏ 1/4 số mục cũ nhất (tối thiểu 8) → thử lại một lần, cùng lối `saveForecast` bên localStorage. ⚠️ Chỉ áp cho kho CÓ TRẦN (`sdfish-tiles-v1` · `sdfish-api-v1` · `sdfish-static-v1` · `sdfish-rsc-v1`); kho vỏ `sdfish-v6` KHÔNG bao giờ đuổi mục vì mục cũ nhất ở đó là `/`, `/ngu-truong`, `vn-coast.v1.json`, font bản đồ.

  (3) **BẢN REDIRECT KHÔNG ĐƯỢC VÀO KHO VỎ**. `Cache.put` không ném với response redirect, nên `installShell` nhận im lặng; bẫy nổ lúc PHỤC VỤ — `caches.match(req)` trả entry `redirected = true` cho request điều hướng (redirect mode `manual`) ⇒ trình duyệt coi là network error ⇒ MÀN TRẮNG offline. Đường "từ mạng" đã gác `opaqueredirect` từ 2026-08-01; đường "từ kho" nay mới gác (vỏ sống-còn: throw; nhóm dock: bỏ qua). Chưa nổ vì không route SHELL nào redirect — nhưng thêm một redirect vào `/`, `/ngu-truong`, `/tau`, `/nguoi`, `/tien`, `/cang` là nổ ngay.

  (4) **QUYẾT ĐỊNH CÓ Ý THỨC — GIỮ NGUYÊN 503 CỦA `/api/tiles`, KHÔNG SỬA.** Có ý kiến rằng 503 làm MapLibre báo lỗi ô; đo lại: repo KHÔNG có `map.on("error")` nào, mà `tileFirst` đã cứu 503 bằng ô đã cất và trả 204 khi kho trống ⇒ hậu quả nhìn thấy được bằng 0. Đổi 503 → 204 thì mất đúng thứ 503 sinh ra để làm: chặn SW cất đè lên ô tốt đang có trong kho (xem mục (3) của biên bản 2026-08-02 phía trên). Giữ nguyên.

  ẢNH HƯỞNG OFFLINE: không thêm request nào lúc mở app/chuyển màn; có đụng `public/sw.js` nhưng KHÔNG đổi `SHELL`, KHÔNG đổi danh sách cache, KHÔNG bump khoá kho ⇒ vẫn chạy bộ bắt buộc `ops/qa-offline-acceptance.md`; không xoá/đè dữ liệu bà con đã tải (`putWithRoom` chỉ đuổi mục ở kho có trần và chỉ khi máy đã báo hết chỗ — không làm gì thì mất bản MỚI). -->

**Last updated**: 2026-08-02

---

## Bảng service ngoài

| Service | Dùng để | Auth | Cấu hình ở đâu | Rate / cache | Khi nó chết thì sao |
|---|---|---|---|---|---|
| **Open-Meteo** (forecast + marine) | Gió/mưa/dông + **sóng theo ngày 1–16** (điểm đi biển); lưới Windy gió/sóng; **lưới DẢI MÀU mây/mưa/nhiệt** (`scalar-field.ts` — `cloud_cover`/`precipitation`/`temperature_2m`); tuyến dầu | Không key | hardcode endpoint trong `lib/sea.ts`, `marine-weather.ts`, `route-weather.ts`, `forecast-grid.ts`, `scalar-field.ts`, `sea-forecast`. **Sóng 16 ngày phải chỉ định `models=ncep_gfswave025`** (best-match sóng chỉ ~8 ngày) — hằng `WAVE_MODEL` | free, cache 6h/1h (sea), client timeout 15s; **lưới tuyến dầu (`route-weather.ts`): cache promise 45 phút theo bbox+ngày-VN** — "Tính lại" không refetch ~1 MB; bản lưu offline `forfish.fc.point.*` theo **ô lưới 0,25°** | Mất mạng → CHỈ lùi về bản lưu **đúng ô lưới của chỗ vừa chạm** (KHÔNG mượn bản của toạ độ khác), gắn cờ `stale` + `savedAt` để UI ghi giờ lưu; chỗ chưa lưu → peek "Chỗ này chưa có số nào lưu trong máy — vuốt lên để thử lại"; ngày sóng thủng ước từ gió (`waveEstimated`); **lưới Windy lùi về bản lưu ĐÚNG khung ngày đã xin** (`forfish.fc.grid.d{N}`) — không có thì báo "máy chưa lưu khung này" + liệt kê khung đang có, KHÔNG đưa lưới khung khác; **tuyến dầu mất sóng lùi về lưới Windy đã lưu** (`route-weather.ts` → `gridToWeatherField` dựng WeatherField từ `loadLongestSavedGrid()`, trục giờ ĐẶT LẠI về 0h hôm nay để bản lưu hôm trước không lệch 24h; `source='grid'` → UI banner "dùng lưới đã lưu, thô hơn + chưa có dòng chảy"), không có lưới nào → báo "mở Ra khơi lúc còn sóng để tải sẵn"; KHÔNG treo |
| **Open-Meteo Ensemble** (GFS-EPS) | Độ bất định dự báo: spread gió 31 thành viên → độ tin từng ngày | Không key, gửi `User-Agent` | `lib/forecast-ensemble.ts` (`ensemble-api.open-meteo.com`, `models=gfs05`) | free, client timeout 15s | `fetchEnsembleUncertainty` trả `null` → độ tin lùi về prior theo tầm ngày (`forecast-quality.ts`), dự báo vẫn chạy |
| **Open-Meteo Archive + Historical-Forecast** (backtest offline) | Học thử độ chính xác: dự-báo-cũ vs thực-tế ERA5 → `forecast-skill.json` | Không key | `scripts/forecast-backtest.mjs` (chạy tay, KHÔNG runtime) — xem [forecast-accuracy.md](forecast-accuracy.md) | — (offline, kết quả commit sẵn) | Không ảnh hưởng runtime; bảng skill thiếu → độ tin/bias lùi về prior |
| **GDACS** (bão) ⚠️ tham số `eventtype=TC` **SỐ ÍT** | Tin bão Biển Đông + **đường đi (track) + vùng ảnh hưởng (polygon)** (`/api/storms`) | Không key | `app/api/storms`, `lib/storms.ts` | server 15s + client 20s; **nguồn hỏng → HTTP 503** (KHÔNG phải 200 kèm `{ok:false}` — sửa 2026-07-31: SW chỉ cất `res.ok` nên 200 giả đã GHI ĐÈ bản tin bão bà con tải ở bờ, ra khơi mất luôn track/vùng ảnh hưởng và cổng chặn tuyến); route trả `checkedAt` (ISO) — **UI BẮT BUỘC đọc**, tin cũ > `STORM_MAX_AGE_MS` = 12h coi như chưa hỏi được (SW cache `/api/*` network-first nên offline vẫn trả `ok:true` bản cũ). **MỐC LƯU TRONG MÁY = `checkedAt`, KHÔNG phải `Date.now()`** (2026-08-02f): `r.ok` không có nghĩa "vừa hỏi được" — SW trả bản kho với status 200 theo ba đường (mất sóng · nguồn 5xx · hết `API_STALE_MS`), đóng giờ máy vào đó là popup "đã lưu gì" khoe "vừa xong" và `savedStormAt()` cho pretrip báo xanh trên tin 6 giờ tuổi | `stormStatus()` quy về 4 nhánh: lỗi/quá-cũ → banner VÀNG "Chưa hỏi được tin bão — máy không có sóng. Nghe thêm đài duyên hải / Icom." (**CẤM** nói "không có bão"); hỏi được & rỗng → "Không có tin bão … (hỏi lúc HH:MM ngày D/M)"; có bão → vẫn hiện + "Tin lúc HH:MM ngày D/M". Lớp bão (track/vùng) ẩn khi không có tin; bản đồ vẫn chạy |

| **NCHMF** (bão/ATNĐ — **nguồn VN, ưu tiên**) | Bản tin bão + áp thấp nhiệt đới bà con nghe trên đài duyên hải (`/api/storms`, gộp với GDACS) | Không key | `lib/storms-vn.ts`, `app/api/storms` | HAI lượt tải: trang liệt kê `https://www.nchmf.gov.vn/kttv/` → bản tin `post` MỚI NHẤT (slug `tin-ap-thap-nhiet-doi*` / `tin-bao*` / `tin-con-bao*`, BỎ `tin-cuoi-cung`); mỗi lượt đồng hồ 15s + `revalidate 1800`; **phải gửi User-Agent** (NCHMF chặn client không khai UA). Là TRANG HTML, không phải API — parse hỏng ⇒ trả `null` = "không hỏi được" (KHÔNG phải "không có bão"); trang không có bản tin nào ⇒ `[]` = trời yên | Cơn VN đứng TRƯỚC trong danh sách; GDACS chỉ thêm cơn cách tâm VN >350 km (khử trùng). `areas` để RỖNG — bản tin VN cho vùng nguy hiểm dạng khung toạ độ, quy thành polygon là tự vẽ thứ nguồn không nói. Cấp gió lấy TRẦN của cấp Beaufort (thà nói mạnh hơn) |

> ⚠️ **HỢP ĐỒNG THAM SỐ GDACS — lỗi thật 2026-08-18, im lặng nhiều tuần.** URL cũ dùng `?eventtypes=TC` (số nhiều) và GDACS trả **HTTP 400 `{"message":"Eventtype is required."}`**; `?eventtype=TC` trả 200 (~565 KB). Nghĩa là `/api/storms` KHÔNG lấy được gì kể từ lúc nguồn siết tham số, mà app vẫn chạy êm: nhánh `!r.ok` trả 503 đúng luật nên màn hình chỉ nói *"Chưa hỏi được tin bão"* — trung thực nhưng **không ai biết nguồn đã chết**. Phát hiện nhờ người của SDVICO báo từ hiện trường (đài báo áp thấp nhiệt đới trên Biển Đông mà app không hiện). Nay: route **log mã lỗi** (`console.error("[storms] GDACS trả", status…)`) để nguồn-đổi-hợp-đồng không còn trông giống nguồn-bảo-trì, và có cổng test `src/lib/__tests__/storms-source.test.ts` canh tham số + đồng hồ + 503 + dòng log.
>
> **GIỚI HẠN CÒN LẠI (chưa vá được bằng code)**: đo GDACS ngày 18/8 sau khi sửa — feed có 122 feature, **3 tâm bão**, mới nhất là `ONE-C-26` (17/8, Đông Thái Bình Dương); **KHÔNG có sự kiện nào trong khung Biển Đông** (99–132°E, 3–27°N). Tức GDACS **không phủ áp thấp nhiệt đới mới hình thành** mà NCHMF/đài duyên hải đã phát. Muốn khớp bản tin bà con nghe đài thì phải THÊM NGUỒN VN (nchmf.gov.vn), không phải sửa tiếp GDACS — quyết định sản phẩm, chờ chủ dự án.
| **VASEP** (giá bến) | Giá nguyên liệu tuần (`/api/port-prices`) | Không key (scrape) | `lib/port-price-source.ts` | cache 24h; **nguồn hỏng/parse vỡ → HTTP 503** (sửa 2026-07-31, chủ dự án: "ra offline thì cứ dùng giá cũ" — 200 kèm `{ok:false}` cũ khiến SW GHI ĐÈ mất bảng giá tuần đã tải ở bờ) | Offline: SW trả bản tuần CŨ, UI in kèm **tuần của bản tin** (`price-board.tsx:122`) nên biết giá tuần nào. Không có bản cũ → bảng giá tĩnh + nhãn "tham khảo", KHÔNG bịa giá |
| **VASEP — KHO bản tin (lịch sử giá, 2026-07-29)** | Chuỗi giá TUẦN cho biểu đồ lịch sử (`/api/port-prices/history`) — gom 2 trang danh sách `?trang=1..2` ≈ 13 tuần ≈ 3 tháng, ngày lấy từ slug URL, parse bằng `parseVasepBulletin` (`lib/port-price-archive.ts` `gatherArchiveWeeks`) | Không key (scrape) | `lib/port-price-archive.ts` + `app/api/port-prices/history` | cache 24h; **fetch song song, mỗi fetch 15s cap + `.catch` bỏ tuần lỗi**; trần `MAX_WEEKS=14` bản/lượt (né timeout) | Tuần parse hỏng → BỎ (khoảng trống, KHÔNG nội suy); <2 điểm / nguồn fail → **HTTP 503** (2026-07-31, để SW giữ chuỗi giá cũ) → sheet báo "chưa lấy được lịch sử", KHÔNG vẽ đường giả |
| **↳ TÍCH LUỸ vào DB** (2026-07-29) | Cron `/api/cron/snapshot-prices` (**Vercel cron `vercel.json` `0 3 * * 6`, thứ Bảy**) UPSERT các tuần gom được vào bảng `price_history` (Supabase) — lịch sử dài dần, không lệ thuộc VASEP còn giữ bản tin cũ. Route ĐỌC DB trước, chỉ lùi gom trực tiếp khi DB <2 điểm | Không key; ghi qua `SUPABASE_SERVICE_ROLE_KEY` | `lib/price-history-store.ts` + `app/api/cron/snapshot-prices` | Bảo vệ `CRON_SECRET` (Vercel tự gắn header); idempotent `(week_end,species_id)`; ⚠️ cron thứ 3 → cần Vercel Pro (Hobby trần 2) | Chưa apply migration 0016 / chưa cấu hình env → ghi 0 dòng (không lỗi), route lùi gom VASEP trực tiếp = hành vi cũ |
| **Petrolimex / giaxanghomnay** | Giá dầu DO (`/api/fuel-price`) | Không key (scrape) | `app/api/fuel-price` | cache 6h; **nguồn hỏng → HTTP 503** (sửa 2026-07-31, cùng lý do VASEP) | Offline: SW trả kỳ giá CŨ, UI in kèm **ngày kỳ giá** (`price-board.tsx:104`). Không có bản cũ → ẩn dòng giá dầu, phần còn lại giữ nguyên |
| **NOAA ERDDAP — LỊCH SỬ** (offline, 2026-07-28) | Dựng **BẢN ĐỒ MÙA VỤ** (`public/data/fish-climatology.v1.json`) + đo **tỷ lệ pha trộn w(d)** (`src/data/fish-blend-weights.json`) | Không key, cùng `ERDDAP_UA` | `scripts/collect-fish-climatology.mjs` (SST `noaacrwsstDaily` — có từ **1985**, lấy stride 10 ngày × 6 năm 2020–2025; phù du `noaacwNPPVIIRSSQchlaMonthly` — ảnh THÁNG sẵn, 2012→nay, stride 6 ≈ 0,25°) · `scripts/fit-fish-blend-weights.mjs` (chl NGÀY `noaacwNPPN20VIIRSDINEOFDaily` cho các mốc lịch sử) | **KHÔNG chạy lúc runtime** — script chạy tay/định kỳ (~1 lần/năm là đủ; mùa vụ đổi rất chậm), ~150 request/lượt, retry 3 lần, timeout 120s | Nguồn chết KHÔNG ảnh hưởng app đang chạy (dữ liệu đã kết tinh thành asset tĩnh). Tháng nào thiếu → điểm 0 → blend tự nghiêng về bản dự báo |
| **NOAA ERDDAP** | SST / phù du / front (dự báo cá) + nước dâng/xoáy (`/api/sea-scalar`) | Không key, **BẮT BUỘC `User-Agent`** (`ERDDAP_UA` trong fish-predict.ts; thiếu → coastwatch trả **403 + HTML** → parse JSON vỡ → `{ok:false}` = cá KHÔNG chạy, chẩn 2026-06-23) | `lib/fish-predict.ts`, `lib/sea-scalars.ts`, `app/api/fish-forecast` (qua **sổ nguồn** `lib/source-registry.ts` — xem §"Sổ nguồn" bên dưới), `app/api/sea-scalar` | cache 6h, **server timeout 20s/lưới** (~250–300 KB/lưới), **client fetch 25-35s** | SST/phù du có **nguồn dự phòng** → một dataset chết vẫn ra bản đồ; chết HẾT nguồn của một trường bắt buộc → route `{ok:false}` → lớp cá pill đỏ "chạm để thử lại", lùi mùa vụ; nguồn treo → fail-fast, KHÔNG treo serverless |
| **HYCOM** (OPeNDAP) | Tầng nhiệt D20 (cá ngừ) + **nhiệt ĐÁY** (loài đáy) + nhiệt 250m | Không key, gửi `User-Agent` (`ERDDAP_UA`) phòng host chặn undici | `lib/hycom.ts` | fetch song song ERDDAP, **timeout 20s** + `.catch→null`; **1 fetch cube → NHIỀU lưới** (`fetchHycomGrids` dùng lại cube cho d20+bottom+deep250, KHÔNG mở DEPTH_RANGE để giữ route 60s) | KHÔNG "chia lại trọng số" nữa (2026-07-26): mốc chuẩn hoá `wMax` CỐ ĐỊNH theo hồ sơ loài → mất D20 làm điểm cá ngừ GIẢM chứ không tăng (trước tăng tới +43%). Loài đáy thiếu nhiệt đáy → fallback SST mặt (không biến mất); treo → null (không treo `await hycomP`) |
| **Copernicus WAV — SÓNG VÉT CUỐI** (ARCO Zarr) | Sóng dự phòng KHÁC NHÀ CUNG CẤP khi CẢ HAI model sóng Open-Meteo chết (cron ghép snapshot) | KHÔNG key (kho công khai) | `lib/copernicus-wav.ts`; dataset `cmems_mod_glo_wav_anfc_0.083deg_PT3H-i` asset `downsampled4` (bucket `mdl-arco-time-015`); PROBE `scripts/copernicus-wav-probe.mjs` — VHM0/VMDR `<i2` scale 0,01 (VMDR offset 180; giải mã bằng `decodeTypedChunk` MỚI, KHÔNG dùng decodeFloat32Chunk); VMDR cùng quy ước "TỚI TỪ" như gió/sóng (khớp Open-Meteo 195° vs 199°) | CHỈ cron gọi KHI bản ghép trống sóng; 2 mốc/ngày (10h+16h VN) ≤6 ngày ≈ ~930 KB/mốc → trần ~11 MB/lượt-vét; tầm nguồn ~+9 ngày; attribution Copernicus khi hiển thị | `fetchWavBackup` trả null → bản ghép giữ nguyên (sóng ngày trống → sea ước từ gió, lưới để trống) — không treo cron |
| **Copernicus — ĐỘ MẶN** (ARCO Zarr) | Lớp dải màu "Độ mặn" theo NGÀY (`/api/salinity`) | KHÔNG key (kho công khai) | `lib/copernicus-salinity.ts` (server) + `app/api/salinity/route.ts`; dataset `cmems_mod_glo_phy-so_anfc_0.083deg_P1D-m` asset `downsampled4` (bucket **`mdl-arco-time-010`**, KHÁC currents `-015`); PROBE `scripts/copernicus-salinity-probe.mjs` | ISR 6h; **≤4 mốc NGÀY** (hôm nay→+3), mỗi chunk ~1 MB (lz4+shuffle, decodeFloat32Chunk dùng chung); tầng mặt = |elevation| nhỏ nhất (trục elevation GIẢM dần); đơn vị **PSU**; attribution "Generated using E.U. Copernicus Marine Service Information" | Route 503 / `fetchScalarField` lùi bản lưu offline + `stale`; chưa lưu → lớp báo "chưa tải được", bản đồ vẫn chạy |
| **ETOPO 2022** (NOAA PIFSC ERDDAP) | Độ sâu đáy (m) — cổng chặn loài XA BỜ khỏi ô cạn (`bathyGridUrl`/`parseBathyGrid`, `SpeciesProfile.offshore`) | Không key, gửi `ERDDAP_UA` | `lib/fish-predict.ts`, `app/api/fish-forecast` (host `oceanwatch.pifsc.noaa.gov`, stride 60 = 0.25°) | TĨNH (đáy không đổi), cache 6h theo route, timeout 20s + `.catch→null` | Không có lưới độ sâu → **KHÔNG bỏ cổng** (2026-07-26): loài `offshore` nhân `DEPTH_UNKNOWN_FIT = 0.5` ("không biết thì bớt chắc chắn") thay vì ×1 — trước đây ×1 nghĩa là NGUỒN CHẾT LÀM ĐIỂM TĂNG, cá ngừ hiện lại sát bờ. Hệ quả đo trên lưới thật: loài xa bờ VẪN trong payload (≥25) nhưng gần hết ô ≥50 → ở sàn hiển thị 50 bản đồ thưa cá xa bờ tới khi ETOPO sống lại. Forecast vẫn chạy |
| **Overpass / OpenSeaMap** | Phao đèn, báo hiệu gần bờ | Không key | `app/api/nautical` | timeout 25s (nguồn chậm) | Lớp phao ẩn; hải đồ + dự báo vẫn chạy |
| **NASA GIBS / tiles vệ tinh** | Ảnh mây, nhiệt độ, phù du nền bản đồ | Không key | `lib/ocean-map.ts` (buildMapStyle) — **cross-origin thẳng, SW KHÔNG giữ được** | tile CDN | Badge "Chưa tải được"; đổi lớp khác được. Mất sóng → lớp ảnh trống, nhưng nền nước + bờ/đảo trong máy vẫn vẽ |
| **EMODnet Bathymetry** (hải đồ độ sâu) | Lớp "Hải đồ độ sâu" (mặc định khi mở bản đồ) | Không key (CC-BY 4.0) | `lib/tile-proxy.ts` (`chart`) → `app/api/tiles/[src]/[z]/[x]/[y]` → `tiles.emodnet-bathymetry.eu`. **KHÔNG gọi thẳng từ client** — đi same-origin để service worker giữ được ô đã xem | z0–12; CDN `s-maxage` 30 ngày (đáy biển không đổi), SW kho `sdfish-tiles-v1` trần 600 ô | **204 chỉ khi ô TRỐNG THẬT** (ngoài dải zoom / upstream 404); upstream 429·5xx·timeout 12s → **503** để SW không cất đè ô tốt (2026-08-02). Mất sóng hoặc nguồn hỏng → SW trả ô đã xem (đồng hồ 8s `TILE_NETWORK_MS`); ô chưa xem thì trống, còn đường đẳng sâu `public/data/isobaths.v1.json` (SW giữ sẵn) vẫn vẽ |
| **OpenSeaMap seamark tiles** (phao đèn) | Lớp phao/đèn/báo hiệu z≥8 | Không key (ODbL) | `lib/tile-proxy.ts` (`seamark`) → `app/api/tiles/[src]/...` → `tiles.openseamap.org` | z8–18; `s-maxage` 7 ngày | Như trên: 204 (trống thật) / 503 (không hỏi được) → lớp phao trống, hải đồ + dự báo vẫn chạy |
| **OpenSeaMap depth WMS** (contour) | Contour 250/500/750…m — proxy `app/api/tiles/contour` + `lib/nautical-layers.ts` | Không key (ODbL) | **CHƯA NỐI vào style** (2026-07-25: route dò lại vẫn trả PNG thật) — trùng vạch với đường đẳng sâu tự sinh ở đúng dải zoom bà con dùng → sẽ rối. Quyết định giữ/bỏ chờ review | — | Không ảnh hưởng: không lớp nào gọi |
| **CARTO Voyager** (nền đường bờ/địa danh) | Bản đồ nền | Không key | `lib/ocean-map.ts` — **giữ cross-origin CỐ Ý**: điều khoản CARTO không cho proxy/cache lại tile | tile CDN, 4 subdomain | Mất sóng → tile không về. Bù bằng **nền tối giản trong máy** (`lib/offline-basemap.ts` + `public/data/vn-coast.v1.json`): lớp nền nước `sea-bg` + hình bờ/đảo. KHÔNG bao giờ để màn hình trắng |
| **Natural Earth 1:10m** (bờ + đảo offline) | Sinh `public/data/vn-coast.v1.json` | Không key (public domain) | `scripts/generate-coastline.mjs` — **chạy tay, KHÔNG runtime** | — (kết quả commit sẵn, 215 KB) | Không ảnh hưởng runtime |
| **Wikipedia tiếng Việt** (tên đảo, 2026-08-07) | Sinh `public/data/vn-islands.v1.json` (~103 đảo tiếng Việt) | Không key (toạ độ/tên = dữ kiện) | `scripts/generate-islands.mjs` — **dữ liệu curated trong script, KHÔNG fetch runtime**; cổng tự kiểm ký tự Hán/CJK + khung VN | — (kết quả commit sẵn, 18 KB) | Không ảnh hưởng runtime — asset tĩnh SW giữ sẵn |
| **OSM Overpass** (chi tiết hải đồ, 2026-08-07) | Sinh phần OSM của `public/data/vn-sea-lanes.v1.json`: fairway (25) · separation/TSS (108) · cable/pipeline (103) · platform (41) · restricted_area (8), **BỎ HẾT tag `name`** | Không key (ODbL) | `scripts/generate-sea-lanes.mjs` — **Overpass lúc BUILD, KHÔNG runtime**; bbox trọn biển VN (4–24N,102–118E); 3 mirror fallback; tuyến lớn vẽ tay trong script | — (kết quả commit sẵn, ~78 KB) | Nguồn chết lúc build → chỉ có tuyến vẽ tay; runtime KHÔNG gọi Overpass (asset tĩnh) |
| **~~fonts.openmaptiles.org~~** (glyph bản đồ) | Chữ/số trên bản đồ (số mét đường đẳng sâu) | — | **ĐÃ BỎ 2026-07-25**: CDN nay trả trang HTML chuyển hướng thay vì `.pbf` → nhãn KHÔNG hiện. Nay **tự host** `public/fonts/{fontstack}/{range}.pbf` (Noto Sans Regular/Bold, OFL — `public/fonts/OFL.txt`) | asset tĩnh same-origin, SW giữ sẵn dải `0-255` | Không còn phụ thuộc bên ngoài |
| **Supabase — ForFish** (`znzgugvfhgmiszqgjulk`) | Auth (SĐT) + DB owner-only (boats/documents/profiles) | publishable + anon (public env) | Vercel env `NEXT_PUBLIC_SUPABASE_*` | — | Env trống → **demo mode** localStorage, app vẫn dùng được (02 §4) |
| **CRM SDViCo gateway** (`exueouggmbjtjvsvpfya`) | Đồ đã mua (`forfish-gateway`) + đăng nhập SĐT+mật khẩu (`auth-gateway`: action `signup`/`sso`, KHÔNG OTP/magic-link/service-key) | sb_publishable key (in-code ALLOWED_KEYS, verify_jwt:false) | Edge Functions `forfish-gateway`/`auth-gateway` (service key tự cấp trong CRM) | client 20s | `useSdvicoAssets` nấc `error` + Thử lại; chưa nối → `unlinked`. ⚠️ CHUYỂN TIẾP — thay bởi webhook + DB riêng ([04 §5b](../04-data-model.md)) |
| **SDWork webhook** (ingest + provision auth) | Nạp KH/thiết bị/vật tư + tạo tài khoản (SĐT+mật khẩu) vào SDFish | HMAC `SDWORK_WEBHOOK_SECRET` (header `x-sdwork-signature`) | `app/api/sdwork/webhook` + `lib/sdwork-webhook.ts` | SDWork đẩy khi đổi | Sai/thiếu chữ ký → 401/503; rớt event → cron đối soát (Đợt 2); app đọc bản đã nạp, không phụ thuộc SDWork lúc KH mở. Đăng nhập = SĐT+mật khẩu, KHÔNG email/OTP |
>
> ✅ **VÁ NGUỒN 2026-08-18b — THÊM NCHMF, KHÔNG BỎ GDACS.** Sửa tham số chỉ chữa được nửa vấn đề: đo lại sau khi sửa, GDACS có 122 feature / 3 tâm bão nhưng **0 sự kiện trong khung Biển Đông**, trong khi NCHMF đã phát *"TIN ÁP THẤP NHIỆT ĐỚI TRÊN BIỂN ĐÔNG"* tâm 19,8°N–117,6°E cấp 6 (đo thật 18/8, `post53205`). Nay route hỏi **cả hai nguồn song song**: tin VN đứng trước (bà con đối chiếu với đài duyên hải), GDACS bù polygon vùng ảnh hưởng + cơn ngoài vùng NCHMF ra tin. Một nguồn hỏng vẫn `ok:true` bằng nguồn kia; **chỉ CẢ HAI hỏng mới 503**. Payload thêm `sources: {nchmf, gdacs}` để soát sau này biết app đang sống bằng nguồn nào. Parser thuần + 19 ca test dựng từ BẢN TIN THẬT (`lib/__tests__/storms-vn.test.ts`).
>
> ⚠️ **NCHMF là HTML, phải coi như nguồn DỄ VỠ**: đổi bố cục trang là parse trượt. Bảo vệ đã có — parse trượt ⇒ `null` ⇒ client nói "chưa hỏi được" (không bao giờ "không có bão"), GDACS vẫn chạy, và log `[storms] NCHMF …` để lại dấu vết. Khi soát định kỳ: chạy `parseToaDo`/`parseCapGio` trên bản tin mới nhất, không tin vào việc "route trả 200".

## Sổ nguồn `/api/fish-forecast` — nhiều nguồn / một trường, SO NGÀY lấy mới nhất

> Yêu cầu chủ dự án 2026-07-26: *"dữ liệu có nhiều nguồn thì down về so ngày (lấy ngày mới nhất), khi 1 nguồn lỗi thì hệ thống vẫn luôn hoạt động được."*
> Luật thuần + test: [`src/lib/source-registry.ts`](../../../src/lib/source-registry.ts) (`resolveField`) · dựng danh sách ứng viên + compute: [`src/lib/fish-forecast-run.ts`](../../../src/lib/fish-forecast-run.ts) (`computeFishForecast`).

### PRECOMPUTE — cron tính sẵn, route chỉ đọc snapshot (2026-07-26)

> Vì sao: các nguồn trên NẶNG + HAY TREO (HYCOM OPeNDAP, Copernicus Zarr) — tính tại chỗ mỗi lần cache lạnh dễ vượt mốc hủy 35s của client → "dự báo cá chưa tải được". Trị tận gốc: tách compute khỏi request của user.

- **Cron** `/api/cron/refresh-fish` (`computeFishForecast()` → `saveFishSnapshot()`) chạy theo lịch: **GitHub Actions** `.github/workflows/refresh-fish.yml` cron `20 */6 * * *` (best-effort, có thể trễ/bỏ run — không sao vì route có fallback) + **Vercel cron** `vercel.json` `0 2 * * *` (Hobby chỉ ~1 lần/ngày → dự phòng; Pro đổi thành `20 */6 * * *` được).
- **Đọc**: `/api/fish-forecast` → `loadFishSnapshot()` đọc bảng `fish_forecast_snapshot` (service-role, `next.revalidate` 30′ giữ ISR). Chưa có snapshot → tự tính fallback (`computeFishForecast`) → KHÔNG bao giờ trắng bản đồ.
- **Ghi đè**: `shouldReplaceSnapshot` ([`src/lib/fish-snapshot-policy.ts`](../../../src/lib/fish-snapshot-policy.ts), thuần, có test) — không lùi ngày, không thay bản tốt bằng bản hỏng.
- **KÍCH HOẠT** (chưa có thì degrade êm về hành vi cũ): apply migration `0005` + env `CRON_SECRET` (Vercel — Vercel Cron tự gắn header — và GitHub Secret trùng) + GitHub Variable `APP_BASE_URL`. `SUPABASE_SERVICE_ROLE_KEY` đã có.
- I/O: [`src/lib/fish-snapshot.ts`](../../../src/lib/fish-snapshot.ts) (server-only, service-role qua `lib/supabase/admin.ts`).

### LƯỚI AN TOÀN thời tiết Open-Meteo — snapshot là FALLBACK, live là chính (2026-07-26)

> KHÁC snapshot cá (snapshot làm CHÍNH vì nguồn nặng/hay-treo). Open-Meteo NHANH + ỔN ĐỊNH nên client vẫn gọi LIVE trực tiếp (tải phân tán theo IP từng máy — đẩy hết lên 1 server sẽ chạm rate-limit Open-Meteo nhanh hơn). Snapshot server chỉ lùi về khi live LỖI + máy chưa có localStorage.

- **Khung snapshot** (2026-07-29): `SNAPSHOT_DAY_SET = [3, 16]` — d3 miễn phí (public) + **d16 premium** (route `/api/weather-snapshot` chặn thật 401/403). Vì sao thêm d16: từ khi bỏ chip chọn khung, màn Ra khơi TỰ đặt tầm theo hạng nên premium LUÔN xin d16 → trước đây họ không bao giờ chạm tới lưới an toàn, gặp "chưa tải được khung 16 ngày" mỗi lần Open-Meteo 429.
- **Cron** `/api/cron/refresh-weather` (Vercel cron `30 2 * * *`, chung `CRON_SECRET`): `fetchSeaLive` 10 cảng + `fetchForecastGridLive(3)` + **`fetchScalarFieldsLive(3)` (2026-07-29 — MỘT fetch ra 5 lớp dải màu mây/mưa/nhiệt/dông/áp suất)** → `saveWeatherSnapshot` → bảng `weather_snapshot` (id `sea:<port>` | `grid:d3` | `scalar:<kind>:d3`). Lý do thêm scalar: lưới 156 điểm × 5 biến nặng theo trọng số Open-Meteo — dính 429 khi client gọi dày; cron chạy từ IP Vercel 1 lần/ngày làm lưới an toàn.
- **Fallback client**: `sea.ts` thứ tự cache-TTL → live → `/api/weather-snapshot?id=sea:<port>` → cache cũ; `forecast-grid.ts` live → localStorage → (chỉ d3) `/api/weather-snapshot?id=grid:d3`; `scalar-field.ts` live → localStorage → (chỉ d3) `/api/weather-snapshot?id=scalar:<kind>:d3`. Độ mặn KHÔNG qua đây (Copernicus server-side sẵn, /api/salinity).
- **Chỉ khung MIỄN PHÍ**: lưới >3 ngày là premium, non-premium không tải từ live → KHÔNG snapshot công khai (kẻo lộ). Cảng snapshot đủ 16 ngày vì client vốn đã tải đủ từ live (không lộ thêm).
- Khoá + whitelist thuần: [`src/lib/weather-snapshot-id.ts`](../../../src/lib/weather-snapshot-id.ts) (có test) — chặn `/api/weather-snapshot` thành proxy đọc bảng tuỳ ý.

**Luật `resolveField` (mỗi luật một test)**: (1) mọi ứng viên chạy **song song** `Promise.allSettled` — không tuần tự, ngân sách route 60 s; (2) bỏ ứng viên lỗi / trả `null` / ngày không parse được; (3) trong số còn lại lấy bản có **ngày dữ liệu MỚI NHẤT**, hoà ngày → ứng viên **ưu tiên cao hơn** (đứng trước); (4) `ageDays` tính theo **ngày Việt Nam** (`isoDateVN`), ngày tương lai kẹp về 0; (5) bản mới nhất mà **quá `maxAgeDays` vẫn TRẢ VỀ** (thà ảnh cũ còn hơn không có) nhưng gắn `stale: true` — KHÔNG âm thầm coi là hiện tại; (6) không ứng viên nào dùng được → `null` (trường bắt buộc → `{ok:false}`, trường tuỳ chọn → bỏ yếu tố).

| Trường | Bắt buộc? | Ứng viên (thứ tự ƯU TIÊN) | `maxAgeDays` | Khi trường này rỗng |
|---|---|---|---|---|
| `sst` | **CÓ** | 1. `noaa-blended-sst` — `noaacwBLENDEDsstDaily` (**kelvin**)<br>2. `noaa-coraltemp-sst` — `noaacrwsstDaily` CoralTemp 5km (**°C, KHÔNG kelvin**) | 3 | `{ok:false}` — không bịa bản đồ cá |
| `chl` (phù du) | **CÓ** | 1. `noaa-viirs-dineof-chl` — `noaacwNPPN20VIIRSDINEOFDaily`<br>2. `noaa-multisensor-dineof-chl` — `noaacwNPPN20S3ASCIDINEOFDaily` (thêm Sentinel-3 OLCI) | 7 (mây che nhiều) | `{ok:false}` |
| `sla` (SSHA/xoáy) | không | `noaa-blended-ssh` — `noaacwBLENDEDsshDaily` | 3 | bỏ yếu tố rìa xoáy + nước lõm lạnh |
| `anom` (dị thường nhiệt) | không | `noaa-crw-sst-anomaly` — `noaacrwsstanomalyDaily` | 3 | bỏ yếu tố nước trồi |
| `currents` (u,v) | không | `copernicus-glo-phy-uv-total` — CMEMS `cmems_mod_glo_phy_anfc_merged-uv_PT1H-i`, `utotal`/`vtotal` 1/12° (ARCO Zarr, asset `timeChunked`); **cặp u+v là MỘT ứng viên**, ngày = ngày UTC của mốc GIỜ đã chọn.<br>**KHÔNG có dự phòng — CỐ Ý**: NOAA `noaacwBLENDEDNRTcurrentsDaily` ĐÃ BỊ GỠ khỏi trường này (xem mục Copernicus bên dưới) | 1 (bước 1 giờ) | **bỏ HẲN yếu tố hội tụ dòng** — thà thiếu còn hơn lùi về nhiễu |
| `hycom` (D20 + nhiệt đáy + 250 m) | không | `hycom-gofs` — 1 cube → 3 lưới (OPeNDAP `tds.hycom.org`, **hay treo** → timeout ngắn `SLOW_SOURCE_TIMEOUT_MS` 12s, KHÔNG phải 20s) | 3 | cá ngừ bỏ yếu tố tầng nhiệt; loài đáy fallback SST mặt |
| `bathy` (độ sâu đáy) | không | `etopo-2022-15s` (PIFSC ERDDAP) — **TĨNH**, ngày = hôm nay, `maxAgeDays = STATIC_MAX_AGE_DAYS` (không bao giờ stale) | — | KHÔNG bỏ cổng: loài xa bờ nhân `DEPTH_UNKNOWN_FIT` 0.5 (điểm trần 50 = sàn hiển thị → không dựng lại được điểm nóng sát bờ) |

**Thêm nguồn mới** = thêm phần tử vào mảng ứng viên trong `route.ts` + một dòng ở bảng này. KHÔNG phải sửa luật. Trước khi thêm **PHẢI fetch thử thật** (đúng bbox/stride, kiểm ĐƠN VỊ — CoralTemp là °C còn Blended là kelvin; sai là cả bản đồ lệch 273°).

**Payload thêm** (không phá cấu trúc cũ): `sources[field] = { id, date, ageDays, stale }` (trường vắng mặt = không nguồn nào dùng được), `dataQuality` 0..1, `targetDate` = ngày dữ liệu dùng lọc mùa vụ.
`dataQuality` = 1 − 0,25/trường **bắt buộc** cũ − 0,05/trường **tuỳ chọn** mất hẳn − 0,025/trường tuỳ chọn có-nhưng-cũ (kẹp [0,1]). CHỈ để hạ kỳ vọng — **KHÔNG nhân vào điểm cá**.

**Tải trọng lên NOAA**: 9 → **11 fetch/lượt tính** (thêm 2 nguồn dự phòng) → **9** sau khi gỡ cặp u,v NOAA khỏi trường `currents` (2026-07-26), cộng ~6 request Copernicus (metadata + 3 trục, cache 24 h + 4 chunk dữ liệu). Tất cả **song song** nên wall-clock ≈ lưới chậm nhất, không cộng dồn. Mỗi lưới ~250–300 KB (không phải "vài MB" như ghi trước đây). ISR 6h + cửa chặn `pretrip-auto` giữ trần lượt gọi như cũ. Nếu về sau thêm nhiều ứng viên nữa mà route chạm 60 s thì đổi chiến lược: chỉ gọi dự phòng khi nguồn chính hỏng (mất luật "so ngày lấy mới nhất" — phải cân nhắc, ghi lại lý do).

## Copernicus Marine ARCO (Zarr) — dòng chảy TỔNG, **ĐANG CHẠY trong `/api/fish-forecast`** (từ 2026-07-26)

> Đọc: [`src/lib/copernicus.ts`](../../../src/lib/copernicus.ts) · kiểm chứng: `npx tsx scripts/copernicus-probe.mjs` · hiệu chỉnh: `npx tsx scripts/conv-copernicus-calib.mjs` · test: `src/lib/__tests__/copernicus.test.ts`.
> Trạng thái: là **ứng viên DUY NHẤT** của trường `currents` trong sổ nguồn. NOAA `noaacwBLENDEDNRTcurrentsDaily` **đã gỡ hẳn** khỏi route (bớt 2 fetch).

| Mục | Nội dung |
|---|---|
| Nguồn | Copernicus Marine Service (CMEMS) — sản phẩm `GLOBAL_ANALYSISFORECAST_PHY_001_024`, dataset `cmems_mod_glo_phy_anfc_merged-uv_PT1H-i_202211`, asset **`timeChunked`** (Zarr v2 trên CloudFerro S3) |
| Auth | **KHÔNG cần key, KHÔNG cần đăng nhập** — kho ARCO công khai (fetch thật: HTTP 200). Nếu về sau đòi đăng nhập thì DỪNG và báo, KHÔNG nhúng secret |
| Attribution | Bắt buộc ghi nguồn khi hiển thị: *"Generated using E.U. Copernicus Marine Service Information"* (giấy phép Copernicus Marine, dùng lại tự do kể cả thương mại, có ghi nguồn) |
| Biến dùng | `utotal`/`vtotal` = dòng **TỔNG** (Eulerian + sóng Stokes + triều). Còn có `uo`/`vo` (Eulerian thuần), `utide`/`vtide`, `vsdx`/`vsdy` |
| Độ phân giải | **1/12° ≈ 9 km** (lat 2041 ô −80…90, lon 4320 ô −180…180 — **hệ có dấu, phải quy đổi**), bước **1 giờ** |
| Dự báo tương lai | **CÓ** — trục `time` chạy tới **+9/+10 ngày** so với hôm nay. Route hiện chỉ lấy mốc GẦN BÂY GIỜ nhất; phần dự báo để dành |
| Định dạng | chunk `[1,1,512,2048]`; nén **blosc(lz4, shuffle byte)**, dtype `<f4`, `fill_value` 9.969209968386869e+36 → NaN. Giải nén **tự viết thuần TS**, KHÔNG thêm phụ thuộc. Hộp biển VN **vắt qua ranh lat 1024** ⇒ **2 chunk lat × 1 chunk lon = 2 chunk/biến** (`chunkSpan` + `assembleWindow`, trần `MAX_DATA_CHUNKS = 6`) |
| Ngân sách | `.zmetadata` 12 KB + trục lat/lon/time (cache 24 h) + **4 × ~958 KB** chunk u/v = **3,83 MB**. Đo thật: **1,2 s (ấm) – 4,0 s (lạnh)**. Timeout **12 s** (`SLOW_SOURCE_TIMEOUT_MS` — nguồn tuỳ chọn hay treo không được kéo route quá hạn client 35s; trước là 20s), mọi lỗi → `null` |
| Khi nó chết | `fetchCopernicusCurrents()` trả `null` → `resolveField` cho `currents = null` → **`buildFishForecast` BỎ HẲN term `conv`**. `sources.currents` biến mất, `dataQuality` 1 → 0,95. **KHÔNG lùi về NOAA** — xem lý do dưới. Đo thật kịch bản chặn host: route vẫn **200 trong 1,17 s**, bản đồ vẫn chạy, ô ≥50 giảm 514 → 454, số loài ≥50 giữ nguyên 23 |
| ⚠️ CẤM | Endpoint MOTU cũ `nrt.cmems-du.eu` **đã ngừng và domain bị người khác chiếm**. Chỉ đi qua STAC `stac.marine.copernicus.eu` → S3 `s3.waw3-1.cloudferro.com` |

### Vì sao GỠ HẲN NOAA địa chuyển khỏi `conv` (không giữ làm dự phòng)

NOAA `noaacwBLENDEDNRTcurrentsDaily` khai `standard_name: surface_geostrophic_*_sea_water_velocity`. Dòng **địa chuyển** về mặt vật lý gần như **KHÔNG PHÂN KỲ** (∂u/∂x+∂v/∂y ≈ 0), nên `-(∂u/∂x+∂v/∂y)` trên nó là **NHIỄU VI PHÂN SỐ**, không phải nước dồn thật. Lùi về nhiễu **tệ hơn** không có yếu tố: bất biến monotonic (f0b907d) bảo đảm thiếu nguồn chỉ làm điểm GIẢM, còn nhiễu thì vẽ điểm nóng **sai chỗ**.

Bằng chứng — **tự tương quan KHÔNG GIAN trễ-1 ô của trường phân kỳ** trên lưới đã cắt về hộp biển VN (nhiễu răng cưa → ≈0 hoặc âm; cấu trúc vật lý thật → dương rõ):

| Nguồn / asset | Bước lưới | RMS(phân kỳ)/RMS(xoáy) | **Tự tương quan phân kỳ** | Tải | Thời gian |
|---|---|---|---|---|---|
| NOAA địa chuyển `noaacwBLENDEDNRTcurrentsDaily` | 0,25° | 0,140 | **−0,029** ← NHIỄU | — | — |
| Copernicus `downsampled4` (1/3°) | 0,3333° | 0,369 | 0,510 | 1,33 MB | 1,0–3,5 s |
| **Copernicus `timeChunked` (1/12°) ← ĐANG DÙNG** | 0,0833° | 0,404 | **0,732** | 3,83 MB | 1,2–4,0 s |

`geoChunked` **loại thẳng**: chunk `[4272,1,16,8]` gom theo THỜI GIAN — lấy MỘT mốc giờ cho hộp VN phải tải 312 chunk × 2,2 MB. Sai kiểu truy cập.

**Chọn `timeChunked`** vì (a) tự tương quan 0,732 > 0,510 — trường phân kỳ có cấu trúc rõ hơn hẳn; (b) 1/12° **MỊN HƠN** lưới cá 0,25° nên hội tụ được tính **trên lưới gốc rồi mới lấy mẫu xuống** (đúng chiều vật lý), còn `downsampled4` 1/3° **THÔ HƠN** lưới đích — làm mượt trước rồi mới đạo hàm; (c) thêm 2,5 MB / ~3 s vào một route có ngân sách 60 s và 9 fetch khác chạy song song là an toàn (đo thật route: 1,27 s → 4,07 s lần lạnh, 0,88 s lần ấm).

### Hiệu chỉnh lại `CONV_FULL_PER_DEG` (bắt buộc — `w.conv` từng tuned trên nhiễu)

`convergenceStrength()` chuẩn hoá theo chênh lệch giữa hai ô KỀ NHAU, nên hằng "mỗi ô" **lệ thuộc bước lưới**. Đổi 0,25° → 1/12° là **cùng một dòng chảy** nhưng chênh mỗi ô nhỏ đi 3 lần. Vì vậy hằng nay ghi theo **ĐỘ**: `CONV_FULL_PER_DEG` (`src/lib/fish-predict.ts`), nhân với `gridStepDeg(cur.u.lats)` lúc chạy — đổi nguồn/độ phân giải không phải chỉnh tay nữa.

Số đo (`scripts/conv-copernicus-calib.mjs`, lưới THẬT, 2 ngày hè + 1 ngày đông, hội tụ thô lấy mẫu về ô cá 0,25°, chỉ phía HỘI TỤ >0):

| | p50 | p90 | p99 | max |
|---|---|---|---|---|
| NOAA địa chuyển (nguồn CŨ) | 0,0664 | **0,2218** | 0,929 | 4,06 |
| Copernicus dòng TỔNG | 0,1289 | **0,4395** | 0,996 | 2,03 |

Hằng CŨ 0,1 "mỗi ô 0,25°" = **0,4/độ** = **1,80 × p90 của chính nguồn nó**. Giữ nguyên tỷ lệ đó trên nguồn mới: 0,4395 × 1,80 ≈ 0,79 → chốt **`CONV_FULL_PER_DEG = 0,8`**.

**KHÔNG lấy thẳng p90 = 0,44** như luật `UPW_SCALE`/`COLD_SCALE`/`THERMO_BAND`: đo trên ĐIỂM CUỐI thì 0,44 làm %điểm nóng **PHÌNH** (21,2→23,2 · 21,0→23,4 · 29,5→31,7). Ràng buộc mạnh hơn là **KHÔNG phình**, nên neo theo **dải động cũ** — ở 0,8 phân bố `convTerm` khớp gần y hệt nguồn cũ (mean 0,1214 vs 0,1216; p90 0,3662 vs 0,3720; std KHÔNG GIAN 0,306 vs 0,210), nghĩa là yếu tố hội tụ **không to lên, chỉ ĐÚNG CHỖ hơn**.

**TRƯỚC/SAU trên cùng dữ liệu** (%điểm nóng s≥50 / tổng ô biển; loài đại diện = số ô ≥25 / ≥50):

| Ngày | | hot% | med | p90 | ngừ vây vàng | cá nục | mực lá | cá mối | ghẹ xanh | #loài ≥50 |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-23 | TRƯỚC (NOAA) | 21,2 | 37 | 62 | 667/221 | 299/84 | 359/48 | 0/0 | 266/90 | 23 |
| | **SAU (Copernicus, 0,8)** | **21,6** | 37 | 62 | 691/215 | 305/83 | 360/48 | 0/0 | 266/94 | 23 |
| | Copernicus HỎNG (bỏ `conv`) | 19,4 | 36 | 60 | 595/196 | 287/77 | 318/40 | 0/0 | 249/88 | 23 |
| 2026-07-24 | TRƯỚC (NOAA) | 21,0 | 37 | 60 | 669/220 | 284/77 | 377/42 | 0/0 | 260/94 | 23 |
| | **SAU (Copernicus, 0,8)** | **21,7** | 37 | 60 | 683/214 | 291/76 | 373/47 | 0/0 | 252/94 | 23 |
| 2026-01-15 (đông) | TRƯỚC (NOAA) | 29,5 | 44 | 57 | 815/108 | 0/0 | 0/0 | 763/344 | 359/110 | 21 |
| | **SAU (Copernicus, 0,8)** | **28,7** | 43 | 57 | 819/91 | 0/0 | 0/0 | 767/342 | 349/116 | 21 |

Không loài nào biến mất (số loài đạt sàn hiển thị giữ nguyên 23 hè / 21 đông ở MỌI kịch bản). `cá mối` 0/0 tháng 7 và `cá nục`/`mực lá` 0/0 tháng 1 là **mùa vụ**, đúng như bản TRƯỚC. Giáp xác giữ nguyên `w.conv ≈ 0,12` (loài ĐÁY, dòng MẶT không gom chúng) — **không đụng**; `w.conv` của 40 loài **không đổi một số nào**, chỉ đổi hằng chuẩn hoá.

**Đo trên API THẬT** (`GET /api/fish-forecast`, dev server, cache `.next` xoá sạch mỗi lần):

| | thời gian | payload | ô trả về | ô ≥50 | med / p90 | #loài ≥50 | `dataQuality` | `sources.currents` |
|---|---|---|---|---|---|---|---|---|
| TRƯỚC | 1,27 s | 344 KB | 2239 | 494 (22,1%) | 38 / 61 | 23 | 1 | `noaa-blended-currents`, ảnh **2 ngày tuổi** |
| **SAU** | 4,07 s (lạnh) · 0,88 s (ấm) | 345 KB | 2237 | 514 (23,0%) | 37 / 61 | 23 | 1 | `copernicus-glo-phy-uv-total`, **ageDays 0** |
| SAU, chặn host Copernicus | 1,17 s | 332 KB | 2210 | 454 (20,5%) | 36 / 60 | 23 | **0,95** | *(vắng mặt)* |


### Copernicus `thetao` (nhiệt mặt) — chỉ dùng ĐỂ ĐO, **KHÔNG vào runtime** (2026-07-26)

> Đọc: [`src/lib/sst-tendency.ts`](../../../src/lib/sst-tendency.ts) · đo lại: `npx tsx scripts/fish-3day-probe.mjs [--date=YYYY-MM-DD]` · bảng kỹ năng: `src/data/copernicus-tendency-skill.json`.

| Mục | Nội dung |
|---|---|
| Dataset | `GLOBAL_ANALYSISFORECAST_PHY_001_024` / `cmems_mod_glo_phy-thetao_anfc_0.083deg_P1D-m_202406`, asset `downsampled4` (bucket `mdl-arco-time-012`) — biến `thetao`, trung bình NGÀY, chọn tầng `elevation` gần mặt nhất |
| Auth / attribution | Như bảng `merged-uv` ở trên: không key, bắt buộc ghi *"Generated using E.U. Copernicus Marine Service Information"* nếu hiển thị |
| Chi phí ĐO THẬT | `.zmetadata` + trục (time/elevation/lat/lon) + **1 chunk ≈ 668 KB cho MỖI ngày** ⇒ D+0..D+3 = **4,58 MB / 7,0–7,3 s** trên dây. Cộng vào route hiện tại (~3,4 s cold) là **gấp ~3 lần thời gian** cho một thứ không đổi được bản đồ |
| Trạng thái | **KHÔNG nối vào `/api/fish-forecast`.** Đo xong thấy kéo nhiệt tới +3 ngày chỉ làm 0,5–1,6 % số ô đổi trạng thái điểm nóng (Jaccard 0,93–0,98, 3 mùa) ⇒ không đáng tiền băng thông lẫn thời gian route. Xem 01-product + 07-design-spec §10.5 |
| Nếu sau này nối vào | `anchoredSstGrid()` đã fail-safe sẵn: thiếu chunk / ô NaN / snap xa > 0,5° / biên độ > 5 °C đều **rơi êm về persistence** (bản đồ chạy y như hôm nay). Phải bọc `.catch → null` như `fetchCopernicusCurrents` và giữ ngân sách route 60 s |

## Quy tắc

1. **Cột "khi nó chết thì sao" là bắt buộc** — đây là cột cần lúc 2h sáng. Mọi nguồn phải degrade, KHÔNG để treo UI hay báo lỗi câm (đã enforce qua roadmap "thất bại lên tiếng").
2. **Token/secret ghi ĐƯỜNG DẪN, không ghi giá trị** — kiến trúc zero-secret: chỉ env public trên Vercel; service key sống trong Edge Function CRM.
3. **Nguồn mới = dòng mới CÙNG commit** với code tích hợp (Doc+Test sync).
4. Đây KHÔNG phải cron/agent thường trực (Vercel serverless + Edge Functions) → không cần runbook start/stop; vận hành = deploy Vercel + Supabase MCP. Sự cố nguồn ngoài → đọc bảng này TRƯỚC khi sửa code.
